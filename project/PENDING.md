# Pendências do Projeto

> Arquivo central de pendências — **resolvidas e não resolvidas**.
> Toda pendência encontrada em qualquer arquivo do projeto deve ser registrada aqui com um ID único.
> Ao resolver uma, marcar como `✅ Resolvida` com a sessão em que foi corrigida.
>
> Última atualização: 2026-09-12 (Sessão 64)

---

## Não Resolvidas

### Decisões em Aberto

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P61 | **Storage Provider seletivo pro agente — desacoplar onde a memória mora de qual identidade/pagamento autoriza isso** — spec trazida pronta pelo usuário (`spec-storage-provider-agente.md`, apagado da raiz e incorporado aqui). Hoje a única forma de sync remoto é `warden-sync`/P37 (Sessão 54), que já assume TruthID como pagador Arweave via `pin()` — a proposta é abstrair isso atrás de duas interfaces novas no core (`StorageProvider`: `read`/`write`/`list`/`delete`/`exportAll`/`importAll`; `AuthProvider`: `getUserId`/`isSubscriptionActive`/`login`/`logout`), de modo que TruthID vire **um plugin opcional** (`DecentralizedVaultProvider`, o único pago por assinatura + único que depende de TruthID) e não uma dependência obrigatória do core. As outras 3 implementações propostas: `LocalFSProvider` (grátis, disco local, default absoluto — hoje é implicitamente isso que o `Vault` já faz, sem essa abstração formal por cima), `RemoteNodeProvider` (grátis, outra máquina do próprio usuário via a rede de nós já planejada — Fase 9/P14), `ManagedCloudProvider` (pago pro Fabio, infra própria tradicional, Stripe puro, sem Web3). Config resolvida por uma factory a partir de um campo único (`storage_provider: "local"|"remote_node"|"managed_cloud"|"decentralized_vault"`), com a UI de escolha "explícita e didática" (não só um dropdown técnico). **Fluxo de migração entre providers** também especificado (export/import + validação de integridade antes de liberar a troca) — a spec pede que pelo menos a interface (`exportAll`/`importAll`) já exista desde o MVP, mesmo com a migração completa ficando pra depois. **Escopo de MVP sugerido na spec, mas explicitamente marcado como "a decidir com Fabio antes de começar a implementação"**: v1 = `LocalFSProvider` + `DecentralizedVaultProvider` (já tem toda a infra do lado TruthID pronta via P37); v2 = `RemoteNodeProvider` (depende da Fase 9 amadurecer); v3 = `ManagedCloudProvider` (novo produto/infra, sem urgência). Fora de escopo da spec: implementação do `RemoteNodeProvider` em si, modelo de billing do `ManagedCloudProvider`, UI final das telas (só o contrato de dados). **Atualizado 2026-09-08 (Sessão 57, continuação 7)**: as duas perguntas em aberto foram confirmadas com o usuário — (1) **escopo de MVP confirmado como sugerido na spec**: v1 = `LocalFSProvider` + `DecentralizedVaultProvider`, v2 = `RemoteNodeProvider` (quando a Fase 9 amadurecer), v3 = `ManagedCloudProvider` (sem urgência); (2) **`warden-sync`/P37 será refatorado** pra virar a implementação concreta de `DecentralizedVaultProvider` (casca fina sobre a lógica de manifest/bundle/pairing já existente, em vez de duplicá-la) — não nasce em paralelo. **Atualizado 2026-09-09 (Sessão 58)**: núcleo técnico implementado — `StorageProvider`/`AuthProvider` novos em `crates/warden-core/src/storage/mod.rs` (`export_all`/`import_all` com default construído sobre `read`/`write`/`list`/`delete`, mesmo padrão de `ModelProvider::chat` sobre `chat_stream`; nomeação `snake_case`, não `exportAll`/`importAll` como na spec em português — convenção Rust do resto do código), `LocalFSProvider` (casca sobre `Vault`, que ganhou um `delete()` novo — antes não existia, `warden-sync`'s `bundle::apply_bundle` contornava isso com `std::fs::remove_file` direto, ajustado pra usar `vault.delete`), `NoAuthProvider` (sem gate nenhum — nenhuma implementação real de `AuthProvider` existe ainda, `warden-truthid` não expõe superfície de checagem de assinatura). `DecentralizedVaultProvider` novo em `warden-sync` (`storage_provider.rs`) — **decisão de escopo confirmada com o usuário antes de codar**: os 6 métodos delegam pro `Vault` local, exatamente como `LocalFSProvider` (Arweave nunca é tocada por `read`/`write`/`list`/`delete`/`export_all`/`import_all`) — `pin()` não tem leitura seletiva e cada publicação exige aprovação física no celular via QR, incompatível com uma trait de CRUD por arquivo sem uma forma de exibir esse QR pro usuário no meio da chamada. **Limitação aceita, documentada no código**: o push/pull real pra Arweave continua exclusivamente através de `SyncEngine::begin_push`/`finish_push`/`pull` (já testados, já wireados nos comandos `/sync` do CLI/desktop/mobile) — nada mudou nesse fluxo; a interface `StorageProvider` existe (piso pedido pela spec pro MVP) mas não substitui o sync interativo ainda. `warden-bootstrap` ganhou `StorageProviderKind` (`local`/`decentralized_vault`/`remote_node`/`managed_cloud`, campo único + factory — não um registry como `providers`, já que só existe um backend ativo por instalação), `FileConfig.storage_provider: Option<StorageProviderKind>` (sem UI ainda, config.toml/env only, mesma postura de `delegate_max_depth`), `resolve_storage_provider` (env `WARDEN_STORAGE_PROVIDER` vence, mas ao contrário de `resolve_flag`/`resolve_delegate_max_depth` um valor não reconhecido é **erro explícito**, não fallback silencioso — trocar onde a memória mora por um typo não deve passar batido), `build_storage_provider` (factory — `RemoteNode`/`ManagedCloud` erram claramente "não implementado ainda, v2/v3"). `bootstrap()` não muda de assinatura — a factory fica solta pra quem monta a infra de sync chamar depois. Efeito colateral bom, habilitado pela extração de `resolve_vault_path` (pública agora, mesma lógica que já existia inline em `bootstrap()`): corrigido um bug real encontrado na pesquisa — o desktop sempre construía `SyncEngine` contra `desktop_default_vault_path()` fixo, **ignorando** `config.vault_path` (o `Orchestrator` do chat já respeitava; o sync sincronizava outro diretório sem avisar ninguém) — e eliminada a duplicação de precedência que o CLI mantinha à mão em `interactive.rs::resolve_vault_path`, agora delegando pro helper compartilhado. `cargo build/test/clippy` limpos pros crates tocados (`warden-core`, `warden-sync`, `warden-bootstrap`, `warden-cli`, `desktop`) — **não** rodado contra `--workspace` inteiro nesta sessão (o disco chegou a 877M livres no meio da sessão, forçando um `cargo clean` de emergência que liberou ~50GB de `target/` mais ~13GB do `mobile/build`/`.dart_tool`/`ios/Pods`/`android/.gradle`; depois disso o usuário pediu explicitamente pra não recompilar crates não tocados nesta fatia). **Atualizado 2026-09-10 (Sessão 59)**: UI "explícita e didática" no Settings implementada — `desktop/src-tauri/src/lib.rs` ganhou `storage_provider: String` em `SettingsSnapshot`/`SettingsFormPayload` (`storage_provider_kind_to_str` faz o round-trip com `StorageProviderKind`), `get_settings` expõe o valor atual (default `"local"` pra config antiga sem o campo), `save_settings` reaproveita `resolve_storage_provider` pra parsear a string do form (tratando-a como se fosse um override de env) e **rejeita explicitamente** `remote_node`/`managed_cloud` mesmo que uma UI futura algum dia deixe selecioná-los — antes disso o campo só existia via `config.toml`/`WARDEN_STORAGE_PROVIDER` de mão, sem tela nenhuma; agora `save_settings` sempre grava o valor escolhido (parou de só "carregar adiante" o que já tava em disco). Frontend: `SettingsView.tsx` ganhou uma seção "Storage" nova com 4 cards de rádio (`StorageProviderPicker`/`STORAGE_PROVIDER_OPTIONS`) — `local` e `decentralized_vault` selecionáveis, `remote_node`/`managed_cloud` mostrados com badge "Coming soon" e `disabled`, com descrição de cada um (incluindo o aviso didático de que escolher `decentralized_vault` **não** liga o backup pro Arweave sozinho — isso continua exclusivamente pela tela de Sync). `types.ts`/`App.tsx` ganharam o campo `storageProvider`/tipo `StorageProviderKind`. Verificação: `cargo check`/`clippy` limpos em `warden-bootstrap` e `desktop` (crates tocados), 40 testes de `warden-bootstrap` passando, `tsc --noEmit` limpo no frontend — não rodado o app de verdade (`cargo tauri dev`) nesta sessão, usuário preferiu não abrir a janela real. **Incidente de toolchain no meio da sessão**: o `rustc` do sistema atualizou sozinho de 1.97.1 pra 1.98.1 via pacman **durante** a verificação (build normal do Arch, não relacionado a este código) — gerou uma sequência de `SIGSEGV`/ICE aleatórios em crates completamente não relacionados (`rav1e`, `tauri-plugin`, `darling_core`, `nom`) até um `cargo clean` (2.9GB liberados, disco seguiu saudável) resolver o mismatch de metadata entre versões; documentado aqui só pra não confundir uma sessão futura que veja esse padrão de novo. **Atualizado 2026-09-10 (Sessão 59, continuação)**: fluxo real de migração implementado. **Decisão de escopo confirmada com o usuário antes de codar**: hoje `LocalFSProvider`/`DecentralizedVaultProvider` são construídos a partir do mesmo `Arc<Vault>` — migrar entre eles é, na prática, um self-copy no-op; mesmo assim o usuário pediu o motor genérico pronto desde já, pro dia que `RemoteNodeProvider`/`ManagedCloudProvider` existirem. `warden_core::storage::migrate(from, to)` novo — `export_all` de `from`, `import_all` em `to`, e **re-exporta de `to` pra comparar byte-a-byte** com o snapshot original antes de declarar sucesso (o `Ok(())` de `import_all` só garante que nenhum `write` retornou erro, não que o conteúdo chegou certo — 2 testes novos provam isso, incluindo um `LossyProvider` de teste que corrompe silenciosamente e é pego pela reconferência). `desktop`'s `save_settings` chama `migrate` sempre que `storage_provider` muda de tipo **e** o anterior é um dos implementados (`storage_provider_kind_is_implemented` — migrar *a partir de* `RemoteNode`/`ManagedCloud` não faz sentido, nunca houve nada armazenado ali de verdade, então nesse caso a troca de config passa direto); só grava a config nova depois que a migração (com a reconferência) passa — uma migração que falha deixa `config.toml` intocado. Não reconcilia arquivos que já existiam no destino mas não estão na origem, nem apaga nada da origem depois — fora de escopo deste MVP. **Atualizado 2026-09-10 (Sessão 59, continuação 2)**: `AuthProvider` real implementado, com um limite importante confirmado com o usuário antes de codar — **não existe assinatura/billing em lugar nenhum do TruthID hoje** (pesquisa no repo inteiro não achou nada), então `is_subscription_active` não pode checar uma assinatura de verdade. `TruthIdAuthProvider` novo em `warden-sync` (`auth_provider.rs`): `get_user_id()` lê o `owner_address` real do `SyncManifest` (pareamento já rastreado por `SyncEngine`); `is_subscription_active()` é **um proxy de pareamento documentado como tal**, não uma checagem de assinatura de verdade — `true` só quando há um `owner_address` pareado; `login()`/`logout()` retornam erro explícito em vez de fingir suportar (a assinatura da trait não recebe parâmetro, mas o pareamento de verdade é QR-mediado e assíncrono — quem precisar parear/desparear de verdade usa `SyncEngine::pairing_host`/`pairing_join` diretamente). `warden-bootstrap` ganhou `build_auth_provider(kind, manifest_path)`, espelhando o despacho por kind de `build_storage_provider` — só `DecentralizedVault` usa `TruthIdAuthProvider`, todo o resto (`Local`/`RemoteNode`/`ManagedCloud`) usa `NoAuthProvider` (nunca erra, ao contrário de `build_storage_provider`). Testado: 3 testes novos em `auth_provider.rs`, 1 em `warden-bootstrap` cobrindo os 4 kinds. `cargo check`/`clippy`/`test` limpos em `warden-sync`+`warden-bootstrap` (32+41 testes). **Continua sem ligação nenhuma em `bootstrap()`/desktop** — mesma postura "maquinário aditivo" que `build_storage_provider` teve antes desta sessão; nenhuma tela ainda mostra status de autenticação. **Ainda em aberto**: `RemoteNodeProvider`/`ManagedCloudProvider` (v2/v3), e a lacuna maior: `DecentralizedVaultProvider`'s `export_all`/`import_all` ainda não tocam Arweave de verdade — ficam idênticos ao `LocalFSProvider` até alguém desenhar como expor o QR de um jeito que uma trait genérica consiga usar; e uma checagem de assinatura de verdade continua bloqueada até existir alguma infra de billing real pro TruthID. **Atualizado 2026-09-10 (Sessão 59, continuação 4)**: escolhido atacar o `RemoteNodeProvider` (v2) — escopo real descoberto e confirmado com o usuário antes de codar: não dava pra construí-lo sem antes a Fase 9.3/9.4 (registro de dispositivos conectados + roteamento de tool call pra um específico) existirem de verdade no `warden-server`, que só suportava "o mesmo dispositivo pedir pra si mesmo" (Fase 7.4) até agora. Feita **só essa fatia** (protocolo + registro + roteamento no `warden-server`, ver `ARCHITECTURE.md`/`PHASE.md` 9.4) — o `RemoteNodeProvider` (`StorageProvider`) em si, e o cliente WS persistente que ele precisaria pra falar com essa nova rota, ficaram pra uma próxima rodada (decisão explícita do usuário: fatiar em vez de tentar tudo de uma vez). **Atualizado 2026-09-10 (Sessão 59, continuação 5)**: essa próxima rodada — `RemoteNodeProvider` real implementado, com plano escrito e aprovado antes de codar (`EnterPlanMode`, a pedido do usuário). `crates/warden-server/src/remote_node.rs` novo: `RemoteNodeClient` (interno) roda uma única task de fundo (`tokio::select!` entre um canal de saída e `conn.recv()`) sobre um `ServerConnection`, com o mesmo alocador de `call_id`/mapa de pendências de `RemoteToolChannel`; `RemoteNodeProvider` (público) implementa `StorageProvider` de verdade com 4 operações fixas (`vault_read`/`vault_write`/`vault_list`/`vault_delete`, conteúdo em base64, `list` devolve `{"paths": [...]}`) — esse é o contrato que um futuro agente-de-nó (lado alvo, ainda não existe) precisa implementar igual. Testado contra um alvo roteirizado (`ServerConnection` puro, mesmo padrão de `device_routing.rs`) em `tests/remote_node_provider.rs` — 6 testes novos: read/write+read/list/delete de verdade, erro do alvo repassado, resposta malformada tratada sem panic. **Escopo confirmado com o usuário antes de codar (dentro do plano aprovado)**: só o lado que chama nesta rodada — segue faltando (1) o agente-de-nó de verdade (processo que rodaria numa segunda máquina física servindo essas 4 operações contra seu próprio `Vault` — nada faz isso ainda, só o alvo roteirizado dos testes), e (2) ligar isso no `build_storage_provider`/`FileConfig` (bloqueado por uma incompatibilidade de assinatura: a factory é síncrona hoje, `RemoteNodeProvider::connect` é necessariamente assíncrono — decisão de design própria, não resolvida aqui). `cargo test -p warden-server` — 33 testes passando (27 + 6 novos), `clippy --all-targets` limpo. **Atualizado 2026-09-10 (Sessão 59, continuação 6)**: ligado no `build_storage_provider` de vez — bloqueio de arquitetura real encontrado e resolvido com aprovação do usuário via plano escrito (`EnterPlanMode`/`ExitPlanMode`, a pedido do usuário desta sessão em diante): `warden-bootstrap` não podia depender de `warden-server` pra usar `RemoteNodeProvider` porque `warden-server` **já** depende de `warden-bootstrap` (o hub precisa de `bootstrap()`/`Orchestrator`) — ciclo impossível no Cargo. Resolvido separando **`crates/warden-server-protocol`** (novo crate) — `protocol.rs`/`client.rs`/`remote_node.rs` movidos pra lá verbatim (só dependem de `warden-core`); `warden-server` (agora só o hub — `server.rs`/`main.rs`/`remote_tool.rs`, este último ficou porque só o hub o usa) re-exporta tudo de lá pra nenhum teste existente precisar mudar import. `warden-bootstrap` passou a depender do novo crate (sem ciclo, já que ele não depende de nada de volta). `build_storage_provider` virou `async fn` (único jeito de construir um `RemoteNodeProvider` de verdade, que precisa de uma conexão de rede real) e ganhou um terceiro parâmetro, `remote_node: Option<&RemoteNodeConfig>` — `RemoteNodeConfig` novo (`server_url`/`device_id`/`device_name`/`auth_key`/`target_device_id`), `FileConfig.remote_node: Option<RemoteNodeConfig>` novo (config.toml/env-only, mesma postura de `delegate_max_depth` — e sem como preencher isso com proveito ainda, já que o agente-de-nó real do lado alvo não existe). `desktop`'s `save_settings` ganhou `.await` nos dois pontos que já chamavam `build_storage_provider` (o fluxo de migração), passando `existing.remote_node.as_ref()` adiante — **sem mudar a UI**: `remote_node` continua rejeitado pelo `save_settings`/mostrado "Coming soon" no Settings, decisão explícita de manter fora de escopo (não tem como preencher os campos do `RemoteNodeConfig` por lá ainda). Verificação: `cargo check`/`clippy --workspace --all-targets` limpos, `cargo test -p warden-server-protocol` (10) + `-p warden-server` (23) + `-p warden-bootstrap` (43, 3 novos pro `build_storage_provider`) todos passando. **Ainda em aberto**: o agente-de-nó real (processo que serviria as 4 operações contra seu próprio `Vault` numa segunda máquina — sem ele, `[remote_node]` no config.toml não tem com quem falar de verdade), `ManagedCloudProvider` (v3), UI de Settings pro `remote_node`, checagem de assinatura real (bloqueada por billing), e a lacuna do push/pull QR-interativo numa trait genérica. **Atualizado 2026-09-10 (Sessão 59, continuação 7)**: o agente-de-nó real construído — `warden-node`, binário novo em `crates/warden-server/src/bin/warden-node.rs` (mesmo padrão `main.rs`/CLI de `warden-server`, resolve vault path via `warden_bootstrap::{load_config, resolve_vault_path}`). Lógica de verdade em `crates/warden-server/src/vault_node.rs` — `connect`/`serve`, conecta como cliente anunciando as 4 tools (`vault_read`/`vault_write`/`vault_list`/`vault_delete`) e despacha cada `ToolCallRequest` pra um `LocalFSProvider` (reaproveitado tal como está — nenhum código de I/O novo, só a marshalling JSON/base64 que já era o contrato documentado pelo `RemoteNodeProvider`). Plano escrito e aprovado antes de codar. **Verificação de ponta a ponta de verdade, não mais roteirizada**: `tests/vault_node_end_to_end.rs` novo — sobe um `Server` real, um `vault_node::serve` real contra um diretório de vault real, e um `RemoteNodeProvider` real do outro lado; cada asserção confere o arquivo de verdade em disco (não só o round-trip do RPC) — write cria o arquivo, read lê o conteúdo real, list mostra os dois arquivos reais, delete some do disco de verdade. `cargo test -p warden-server` — 25 testes (23 + 2 novos), `cargo check`/`clippy --workspace --all-targets` limpos, `cargo run --bin warden-node -- --help` confirma o binário. `PHASE.md` Fase 9.5 marcada `[x]` (escopado às 4 tools de vault, não um nó genérico de qualquer tool ainda). **Ainda em aberto (antes desta atualização)**: `ManagedCloudProvider` (v3), UI de Settings pro `remote_node` (incluindo os campos de `RemoteNodeConfig`), checagem de assinatura real (bloqueada por billing), a lacuna do push/pull QR-interativo numa trait genérica, e qualquer história de deploy/systemd/empacotamento pro `warden-node` rodar de verdade numa máquina remota. **Atualizado 2026-09-11 (Sessão 60)**: a UI de Settings pro `remote_node` foi feita — `desktop/src-tauri/src/lib.rs` ganhou `RemoteNodeConfigPayload` (IPC, camelCase) e `SettingsSnapshot`/`SettingsFormPayload` ganharam `remote_node: Option<RemoteNodeConfigPayload>`; `save_settings` parseia os 5 campos (`server_url`/`device_id`/`device_name`/`auth_key`/`target_device_id`) com validação tudo-ou-nada (se algum estiver preenchido, os 5 precisam estar — evita gravar uma config pela metade que nunca vai conseguir conectar) e passou a **gravar de verdade** o valor (antes só existia hand-edit de `config.toml`). A rejeição explícita de `save_settings` que antes cobria `remote_node`/`managed_cloud` juntos ficou só com `managed_cloud` (v3, segue sem implementação); selecionar `remote_node` sem preencher os 5 campos agora erra com uma mensagem própria em vez de deixar passar. **Bug real encontrado e corrigido no caminho**: o passo de migração já existente (Sessão 59) construía o `to_provider` da migração com `existing.remote_node` (a config *antiga*) mesmo quando o destino novo era `remote_node` — corrigido pra usar `config.remote_node` (o valor recém-salvo nesta mesma chamada), senão trocar pra `remote_node` preenchendo os campos pela primeira vez tentaria conectar com a config errada (vazia/antiga). `storage_provider_kind_is_implemented` ganhou `RemoteNode` no conjunto "implementado" (tem um `StorageProvider` de verdade agora), então migrar **pra fora** de `remote_node` também aciona o export/import/reconferência de verdade, não só troca a config. Frontend: `STORAGE_PROVIDER_OPTIONS`'s `remote_node` perdeu `comingSoon` (agora selecionável) com a descrição reescrita explicando a dependência de um hub `warden-server` alcançável + um `warden-node` rodando na máquina alvo; `RemoteNodeForm` novo (mesmo padrão condicional que o campo "Base URL" do `ProviderCard` pro `openai_compatible`) renderiza os 5 campos (o `auth_key` via `ApiKeyField`, com toggle de revelar) sempre que `storage_provider === "remote_node"` está selecionado; `types.ts` ganhou a interface `RemoteNodeConfig`, `Settings.remoteNode`. Verificação: `cargo check`/`clippy -p desktop --all-targets` limpos, `cargo test -p warden-bootstrap` (43 testes, nenhuma mudança de comportamento no crate em si) segue verde, `npx tsc --noEmit` e `npm run build` limpos no frontend. **Não** verificado com Playwright/Chrome real desta vez — a extensão do Claude in Chrome não estava conectada neste ambiente (sem browser disponível), diferente das sessões anteriores que conseguiram esse tipo de verificação; ficou só na checagem estática (`tsc`/build) + revisão manual do diff. **Ainda em aberto**: `ManagedCloudProvider` (v3), checagem de assinatura real (bloqueada por billing), a lacuna do push/pull QR-interativo numa trait genérica, e um teste de ponta a ponta de verdade contra um `warden-server`+`warden-node` reais rodando (não só a suíte automatizada de `warden-server`/`warden-server-protocol` já existente) — nenhum hub real disponível neste ambiente pra isso. **Atualizado 2026-09-11 (Sessão 60, continuação)**: um buraco de segurança real do `RemoteNodeProvider`/`warden-node` foi fechado — até aqui, qualquer dispositivo com o `auth_key` compartilhado conseguia rotear `CallDeviceTool` pra (ou como) qualquer outro `device_id`, sem nenhuma noção de "este dispositivo específico foi autorizado". Resolvido como Fase 9.3 (pareamento persistente), ver `PHASE.md`/`ARCHITECTURE.md` — `CallDeviceTool` agora exige `Approved` (registro persistido em `~/.config/warden/devices.json`, gerenciado por `warden-server devices list/approve/revoke`) tanto do chamador quanto do alvo; `Hello`/`Chat` continuam sem exigir aprovação prévia. **Atualizado 2026-09-11 (Sessão 60, continuação 2)**: UI de workspace no desktop (`PHASE.md` Fase 9.6) — `WorkspaceView.tsx` novo lista/aprova/revoga dispositivos do `PairingStore` direto (desktop ganhou dependência no crate `warden-server`, assumindo mesma máquina que o hub — decisão de escopo confirmada com o usuário antes de codar, ver `ARCHITECTURE.md`). Sem protocolo WS admin novo — fica pra quando um cenário multi-máquina de verdade existir. | `spec-storage-provider-agente.md` (trazido pronto pelo usuário), incorpora P37/P24 | 🟠 Média |
| P4 | **Controle de custo/rate limit** — onde e como limitar chamadas de modelo por usuário/período. **Atualizado 2026-08-09**: a parte de *tracking* (capturar e persistir tokens por mensagem) foi resolvida na 5.8 (`warden_core::model::Usage`, `ConversationMessage.usage`) — o que resta aqui é só a parte de **limite de verdade**: rate limiting por tool/tempo e/ou teto de gasto configurável que bloqueie novas chamadas, nenhum dos dois implementado ainda. **Atualizado 2026-09-05 (Sessão 49)**: usuário pediu duas superfícies novas em cima do `Usage` já trackeado (nenhuma implementada ainda) — (1) **`/usage` no `warden-cli`**: comando de barra pra ver quanto foi gasto (tokens/custo) na sessão atual do terminal; (2) **página "home" no app desktop** com dashboards — tokens gastos, custo médio por token, quebra por agente, modelos mais usados — "bonitinho, com bons dashboards", e explicitamente quer que **a própria IA tenha acesso a esses dados** (não só o usuário visualizando). A (2) precisa de mais definição antes de implementar (usuário disse "depois complementamos mais essa ideia") — em aberto: de onde vêm os dados agregados (`Conversation.messages[].usage` já persistido por conversa, mas não há agregação nem por agente nem por provider hoje), se "custo" precisa de uma tabela de preço por modelo (não existe ainda, só contagem de tokens), e que forma de acesso a IA teria (tool nova tipo `usage_stats`? contexto injetado automaticamente?). **Atualizado 2026-09-05 (continuação da Sessão 49)**: item (1) implementado — `/usage` mostra tokens de prompt/resposta/total acumulados na sessão atual do terminal (`CliSession.usage_total`, somado a cada turno em `run()`, nunca persistido — reseta a cada `warden` novo). Só tokens, sem `$`: decisão deliberada, não existe tabela de preço por modelo ainda (mesma lacuna listada acima pro item (2)). Teste de ponta a ponta com acúmulo real (duas mensagens de verdade, conferir a soma) não foi possível nesta sessão — sem chave de API real disponível no shell do agente, só dá pra confirmar via pty que o estado vazio (`/usage` antes de qualquer mensagem) renderiza certo e que o comando roteia sem regredir o tab-completion existente; fica pro usuário confirmar o acúmulo de verdade numa janela real. **Atualizado 2026-09-05 (continuação da Sessão 49, mais uma vez)**: item (2) implementado, com escopo confirmado com o usuário antes de codar (3 perguntas: fonte dos dados → agregação sob demanda, sem novo índice persistido; custo em `$` → não, só tokens por enquanto; acesso da IA → tool nova, não contexto injetado sempre). Nova tool `usage_stats` (`crates/warden-bootstrap/src/usage.rs`) — `aggregate_usage(&[Conversation]) -> UsageSummary` soma `Usage` de toda conversa persistida (desktop/Telegram/WhatsApp), quebrado por `agent_id`/`provider_id`; registrada em `bootstrap()` junto dos outros tools, então qualquer canal (não só desktop) ganha o modelo respondendo "quantos tokens eu já usei" sob pedido, sem custo de contexto nas mensagens que não tocam no assunto. **Limitação aceita e documentada no código**: `Conversation.agent_id`/`provider_id` guardam só a *última* seleção da conversa inteira (os seletores do desktop), não por mensagem — uma conversa que trocou de provider no meio tem todo o uso atribuído ao provider atual, não ao que rodou de fato em cada mensagem; corrigir isso pediria gravar provider/agente por `ConversationMessage`, que não existe hoje, fora de escopo desta agregação sob demanda. Nova tela "Usage" no desktop (`UsageView.tsx`, ícone novo `ChartIcon`, terceiro item no rodapé da sidebar ao lado de Settings) — 5 stat tiles (tokens totais/prompt/resposta, chamadas de modelo, conversas) + duas listas de barra horizontal (por agente, por provider), mesma cor de acento única (uma barra = uma categoria da mesma métrica, sem precisar de legenda), valor sempre na ponta da barra — layout revisado contra a skill de dataviz do projeto (form: stat tile pro headline, bar chart pra quebra categórica; specs de marca: `<=24px` de espessura, ponta arredondada, rótulo direto na ponta, sem hover já que todo valor já é rotulado). `Usage.add_assign` novo em `warden-core` (`impl AddAssign<&Usage> for Usage`), reaproveitado tanto pelo `/usage` do CLI (Sessão 49, parte anterior) quanto por `aggregate_usage` — elimina a duplicação que teria surgido. `cargo build/test/clippy --workspace` limpos (5 testes novos em `usage.rs`: soma total, quebra por agente/provider, tool com dir ausente, round-trip do tool lendo do disco); `tsc`/`npm run build` do desktop limpos. **Verificado via Playwright headless contra o dev server real do Tauri** (não só harness estático) — `window.__TAURI_INTERNALS__.invoke` mockado por `addInitScript` pra simular `usage_summary`/`get_settings`/`list_conversations` sem precisar de uma janela nativa nem dados reais persistidos: clique no botão "Usage" da sidebar, tiles e barras renderizando com números compactos (`189.6K`, `120K`) em claro e escuro sem erro de console, e o estado vazio ("no usage recorded yet") também conferido. **Teste de ponta a ponta com dados reais de uma janela nativa** (não mockado) ainda não feito — mesma lacuna de sempre, sem chave de API real neste shell pra gerar uso de verdade. Fecha o pedido 3 da Sessão 49 | Visão original | 🟠 Média |
| P6 | ~~**Estratégia de busca semântica**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 56 (2026-09-08) |
| P8 | **Canal Terminal cross-platform + lançamento de agentes** — como estruturar um canal terminal completo (tipo Claude Code, não focado em programação) rodando em Linux/Windows/Mac; qual crate de TUI (se houver, ex. `ratatui`); e como "lançar agentes" se relaciona com sub-agentes leves (1.8) vs sub-agentes autônomos (fora de escopo v1). **Atualizado 2026-08-04**: a tool `shell` em si (Fase 5.5) já está implementada e disponível de qualquer canal que passe por `bootstrap()` (CLI, desktop) — o que resta em aberto aqui é só a arquitetura do canal Terminal dedicado (TUI etc.), não mais a tool. **Atualizado 2026-08-09**: primeiro passo de UX dado no `warden-cli` — histórico de linha (`rustyline`), markdown renderizado (`termimad`), spinner (`indicatif`), decisão explícita do usuário de **não** ir pro `ratatui`/TUI de tela alternativa por enquanto (ver `ARCHITECTURE.md`). O que resta em aberto: streaming de resposta de verdade (exigiria mudar `ModelProvider` em `warden-core`), e o TUI completo em si, se algum dia fizer falta. **Atualizado 2026-08-29**: usuário reafirma que a UX "tava feio pra krl" — mesmo com o polish das Sessões 31/33 (confirmado funcionando via pty na Sessão 34), o patamar desejado ainda não foi alcançado. Priorização explícita: **não mexer agora** — voltar a isso depois do Desktop (Fase 6 polish) e do MCP (Fase 5), ver `ROADMAP.md`. Quando retomado, vale perguntar ao usuário o que especificamente ainda incomoda (não ficou claro nesta sessão) antes de tentar de novo. **Atualizado 2026-09-04 (Sessão 46)**: usuário pediu explicitamente pra deixar o `warden-cli` "com cara de Claude Code, mas mais bonitinho e menos focado em código" — perguntado o que isso envolvia, confirmou querer os quatro itens juntos (caixa de input com borda, banner, streaming de resposta de verdade, barra de status com tempo decorrido) e, ao ser avisado do custo real (mudar `ModelProvider`/os 3 providers e reabrir a decisão de não usar `ratatui`), escolheu ir fundo mesmo. Implementado nesta sessão: `ModelProvider` ganhou `chat_stream` (streaming de verdade via SSE nos 3 providers — OpenAI/Anthropic/Gemini — `chat()` virou um método default que drena o stream, então Telegram/WhatsApp/Desktop/`DelegateTool` não mudaram nenhuma linha de código de produção, só os 7 mocks de teste); `warden-cli/src/interactive.rs` reescrito com `ratatui` em modo *inline viewport* (não fullscreen — scrollback do terminal continua normal) pra caixa de input com borda e caixa de "pensando" (spinner + tempo decorrido + Ctrl+C pra interromper), com um editor de linha próprio (`rustyline` saiu) e resposta impressa como texto puro conforme chega (sem markdown ao vivo, aceito como troca deliberada por "menos foco em código"). Ver `ARCHITECTURE.md` pros detalhes de design. **Teste de ponta a ponta numa janela real ainda não feito** — registrado como P34. **Atualizado 2026-09-05 (Sessão 49)**: usuário pediu comandos de barra (`/`) no `warden-cli`, trazendo o máximo possível da Settings do desktop pro terminal — `/models` (listar/usar/resetar/cadastrar/editar/remover provedor) e `/agents` (listar/usar/criar/editar/remover agente nomeado com persona), mexendo no mesmo `config.toml` do desktop, com efeito imediato na sessão (sem restart). Fecha a parte de "lançamento de agentes" desta pendência pro eixo leve/config-driven — sub-agentes autônomos seguem fora de escopo v1. Ver `ARCHITECTURE.md` pros detalhes de design (estado de sessão, cascade de rename/delete) | `ROADMAP.md`, pedido do usuário 2026-08-02, reforçado 2026-08-29, 2026-09-04 e 2026-09-05 | 🟠 Média |
| P9 | **App "Copilot" no SO** — launcher leve tipo Spotlight/PowerToys Run, cross-platform (Linux/Windows/Mac), roda em segundo plano, atalho global abre campo de busca pra apps/arquivos/pastas do sistema + acesso rápido ao Warden. Opt-in/configurável. Relação com a Fase 6 (App Desktop) ainda não definida — modo do mesmo app ou fase própria | `ROADMAP.md`, pedido do usuário 2026-08-02 | 🟡 Baixa |
| P10 | **Dashboard de custo & gerenciamento de chaves de API** — UI de consumo de tokens, custo estimado por provedor/modelo, cadastro/gerenciamento de chaves. Relacionado a P4 (controle de custo/rate limit), que é a parte de backend disso | `ROADMAP.md`, pedido do usuário 2026-08-02 | 🟠 Média |
| P11 | ~~**Tela de gerenciamento de integrações MCP**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 35 (2026-08-29) |
| P12 | **"Warden API"** — chave de API criada facilmente **dentro do próprio app**, hospedada no "app host" do Warden (o app que já está rodando no device do usuário) — não é um serviço à parte. Totalmente autossuficiente e **opcional**: não precisa de servidor central pra existir, a menos que o usuário queira usá-la de fora do próprio device (aí entra em jogo P14). Quem integra escolhe o Warden, não o modelo — a abstração `ModelProvider` já vem com o contexto do vault embutido | `ROADMAP.md`, pedido do usuário 2026-08-02 | 🟡 Baixa |
| P13 | **Integração via MCP com o ecossistema do usuário** — Practice Valuation (rebrand pra Anchor) e TruthID. Depende desses projetos terem um lado MCP pronto — bloqueado por fora do Warden. **Atualizado 2026-08-04**: o lado Warden (client MCP, Fase 5.2) já está pronto — basta o Anchor/TruthID exporem um server MCP e adicionar a entrada em `mcp_servers` no config; o bloqueio que resta é inteiramente do lado desses outros projetos | `ROADMAP.md`, pedido do usuário 2026-08-02 | 🟡 Baixa |
| P15 | **Conectores MCP genéricos / fáceis de criar** — não ficar restrito a integrações MCP que já existem prontas no mercado. O usuário quer facilitar que qualquer pessoa conecte o software dela (mesmo sem um MCP server pronto pra ele) com o Warden, e o agente consiga criar/ler algo nesse software. Precisa definir o mecanismo — gerador de conector a partir de API/OpenAPI spec? Template guiado por UX? | `ROADMAP.md`, pedido do usuário 2026-08-02 | 🟡 Baixa |
| P16 | **Sistema de "Skills"** — usuário quer um conceito de skills no Warden, criáveis pela UX (não só por código), no espírito das Skills do Claude — pacotes de instrução/capacidade reutilizáveis. Arquitetura ainda não definida: como isso difere de `Tool` (código) e como interage com Sub-agentes (P8) | `ROADMAP.md`, pedido do usuário 2026-08-02 | 🟡 Baixa |
| P17 | **Dependência de Node.js/`npx` em runtime** — desde a 5.3 (`web_search` via `tavily-mcp`), o Warden passou a depender de Node.js instalado no PATH pra uma capacidade built-in, não só pra servers MCP configurados opcionalmente pelo usuário (5.2). Se `npx` não existir, o erro hoje é só o erro cru do SO (ex. "No such file or directory") propagado pela mensagem de `register_mcp_server_tools` — funcional, mas não é uma mensagem acionável tipo "instale o Node.js". Vale revisitar se isso incomodar no onboarding (ex. detectar a ausência de Node.js e dar uma dica melhor). **Atualizado 2026-08-09**: confirmado mais relevante ainda — a 5.7 (integração Google, `@aaronsb/google-workspace-mcp`) também é `npx`-based, escolhido deliberadamente por isso mesmo (evitar introduzir um segundo runtime, `uv`/Python, ver `ARCHITECTURE.md`) | `ARCHITECTURE.md` (decisão da 5.3), sessão 2026-08-04 | 🟡 Baixa |
| P18 | **Uso de tokens do `DelegateTool` não é contabilizado na conversa pai** — desde a 5.8, `Orchestrator::handle_message` devolve `MessageOutcome{content, usage}` com o total de tokens da mensagem, mas `DelegateTool::call` (`tool/delegate.rs`) só repassa `result.content` pro modelo — o `usage` da chamada recursiva ao sub-orchestrator é descartado. Fechar isso exigiria mudar a trait `Tool::call` (hoje devolve só `serde_json::Value`) pra carregar uso também, uma mudança de superfície maior — fora do escopo mínimo da 5.8 (só tracking, sem endurecer a trait) | `ARCHITECTURE.md` (decisão da 5.8), sessão 2026-08-09 | 🟡 Baixa |
| P19 | **Respostas do Telegram são texto puro, sem MarkdownV2** — decisão explícita do usuário na Fase 2: o MarkdownV2 do Telegram tem sintaxe própria (diferente de CommonMark) e a API rejeita a mensagem inteira se um caractere reservado não for escapado direito. Um conversor de verdade (CommonMark do modelo → MarkdownV2 do Telegram) é trabalho substancial — negrito/listas/code block ficam sem formatação nas respostas do bot até isso ser feito | `ARCHITECTURE.md` (decisão da Fase 2), sessão 2026-08-09 | 🟡 Baixa |
| P25 | ~~**Client MCP (`McpToolProvider`) só fala transporte stdio**~~ | ✅ Resolvida (parcialmente — ver P26) — ver seção "Resolvidas" abaixo | Sessão 36 (2026-08-31) |
| P26 | ~~**Sem client OAuth pro transporte HTTP do MCP**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 37 (2026-08-31) |
| P21 | **Sem suporte multimodal (imagem/áudio/documento)** — a Fase 3 (WhatsApp) trata mensagens de mídia só com degradação graciosa (resposta fixa, sem chamar o orchestrator), decisão explícita do usuário. Suporte de verdade exigiria mudar `ModelProvider`/`Message` (`warden-core`) pra multimodal — toca os dois providers (OpenAI, Gemini), não é mudança de canal. O Telegram (Fase 2) está pior ainda nesse ponto: mensagem sem `text` (foto/áudio/documento) é **silenciosamente ignorada** (`process_updates` só faz `continue`), nem a degradação graciosa que o WhatsApp ganhou — vale alinhar os dois quando isso for revisitado | `ARCHITECTURE.md` (decisão da Fase 3), sessão 2026-08-09 | 🟡 Baixa |
| P22 | ~~**Suporte a múltiplos provedores de modelo**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 35 (2026-08-29) |
| P23 | ~~**Gerenciamento de API keys no desktop é só um campo de texto por provedor**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 35 (2026-08-29) |
| P30 | **Input de voz (P28/Sessão 41) sem teste de ponta a ponta** — mesma lacuna que P29 (imagem): a implementação (gravação nativa via `cpal` no lado Rust, IPC `start_recording`/`stop_recording`/`transcribe_audio`, chamada real ao Whisper) foi verificada só com `cargo build/test/clippy`, `tsc`/build do frontend e screenshot de layout — nenhuma chave de API (Whisper) disponível no shell do agente. **Atualizado 2026-09-01**: a tentativa 1 (`getUserMedia` do browser) foi testada de verdade pelo usuário e confirmada quebrada (WebKitGTK nega a permissão por padrão, limitação conhecida do Tauri) — trocada por captura nativa via `cpal` na mesma sessão, ver `ARCHITECTURE.md`. **Bloqueio adicional confirmado pelo usuário**: não vai conseguir uma chave da OpenAI tão cedo — o teste de ponta a ponta (Settings → colar chave Whisper → gravar → checar transcrição) fica adiado pra quando isso mudar, não é prioridade agora. Se uma chave da OpenAI realmente não entrar nos planos do usuário a médio prazo, vale revisitar a decisão de depender do Whisper como STT (`ARCHITECTURE.md`) — alternativa a considerar futuramente: transcrição local (ex. `whisper.cpp`/`whisper-rs`), sem depender de nenhuma API paga, mas maior binário/dependência nova | `ARCHITECTURE.md`/`PENDING.md` P28 (Sessão 41) | 🟡 Baixa |
| P31 | **TTS na resposta (P28/Sessão 42) sem teste de ponta a ponta** — mesma lacuna que P29/P30: a implementação (`crates/warden-core/src/speech.rs`, IPC `synthesize_speech`, botão de áudio por mensagem) foi verificada só com `cargo build/test/clippy`, `tsc`/build do frontend e screenshot de layout (harness estático, claro e escuro) — sem chave da OpenAI disponível no shell do agente pra de fato sintetizar áudio e confirmar que toca corretamente. Falta: abrir o app com uma chave Whisper/voz real configurada, clicar no botão de uma resposta, confirmar que o áudio sintetizado corresponde ao texto e que o toggle tocar/parar funciona na janela nativa de verdade | `PENDING.md` P28 (Sessão 42) | 🟡 Baixa |
| P27 | **Integração com Discord** — usuário quer, sem urgência ("acho que vai ser útil no futuro"). Duas frentes independentes, igual P11 foi pro MCP: (a) **Warden como bot no Discord** — novo canal no espírito do Telegram/WhatsApp (Fases 2/3): crate próprio (`warden-discord`?), gateway/websocket (não é polling HTTP como o Telegram nem sidecar Node como o WhatsApp — protocolo próprio da Discord API), histórico de conversa por canal/DM; (b) **Discord como MCP server** — mesmo mecanismo de preset já usado pro Slack/Notion/GitHub (`[[mcp_servers]]`), provavelmente via algum server MCP de Discord já pronto no mercado (`npx`-based, a checar), sem código novo no Warden. Usuário escolheu (Sessão 38) registrar as duas sem implementar nenhuma agora | `ROADMAP.md`, pedido do usuário 2026-08-31 | 🟡 Baixa |
| P29 | **Anexo de imagem (P28/Sessão 40) sem teste de ponta a ponta contra um provider real** — a implementação (`Attachment` no core, codificação multimodal nos 3 providers, IPC/UI do desktop) foi verificada só com `cargo build/test/clippy`, `tsc`/build do frontend e screenshot de layout (Playwright) — nenhuma chave de API estava disponível no shell do agente pra de fato mandar uma imagem pra um provider e confirmar que ele "enxerga" o conteúdo. Falta: abrir o app (`npm run tauri dev` já testado rodando), anexar uma imagem de verdade, perguntar algo sobre ela ("o que tem nessa imagem?") pra cada provider configurado (pelo menos um dos três — Gemini/OpenAI/Anthropic), confirmar que a resposta reflete o conteúdo real da imagem. **Atualizado 2026-09-01**: usuário pediu pra adiar esse teste (junto com P30) pra mais tarde, sem prioridade imediata | `PENDING.md` P28 (Sessão 40) | 🟡 Baixa |
| P34 | **CLI com streaming real + ratatui (Sessão 46) sem teste de ponta a ponta numa janela real** — a implementação (streaming SSE nos 3 providers, `Orchestrator::handle_turn_streaming`, `warden-cli/src/interactive.rs` reescrito com `ratatui` inline viewport + editor de linha próprio) foi verificada só com `cargo build/test/clippy --workspace` (48 testes no `warden-core`, incluindo parsing SSE por provider e o editor de linha isolado) — mesma limitação já registrada antes (Sessão 34) pro polish anterior do CLI: não dá pra automatizar teclado/terminal raw-mode a partir daqui (sem `xdotool`/`wtype` funcionando, e mesmo esses não testariam a experiência de digitação de verdade). **Atualizado 2026-09-04 (Sessão 47)**: usuário testou de verdade numa janela real e confirmou exatamente esse risco — travou em "pensando" e nunca voltou. Causa raiz achada reproduzindo com um pty real (Python `pty`+`select`, simulando um terminal que responde à query de posição do cursor): `ratatui` faz uma leitura síncrona e bloqueante de `ESC[6n` (posição do cursor) toda vez que o `Terminal` é construído ou que `.clear()` é chamado (usado a cada turno, pra apagar a caixa de "pensando"); essa leitura e o `EventStream` assíncrono do crossterm disputam o mesmo lock global interno (`INTERNAL_EVENT_READER`) — a thread de fundo do `EventStream`, uma vez usada, fica bloqueada indefinidamente segurando esse lock enquanto espera a próxima tecla (o normal durante um turno, já que ninguém digita enquanto o modelo responde), fazendo a query de cursor estourar o timeout fixo de 2s do crossterm sempre que não há tecla nenhuma sendo pressionada — comportamento documentado no próprio doc-comment de `crossterm::cursor::position()`, não uma falha isolada da implementação. **Corrigido**: `EventStream` removido de vez (`crossterm::event::{poll, read}` síncronos com timeout curto de 30ms no lugar — nunca segura o lock além do próprio timeout, então a query de cursor sempre encontra ele livre); um único `Terminal` construído uma vez por sessão (não mais um novo por turno) reaproveitado por `read_line`/`run_turn`. Dependência `futures-util` e a feature `event-stream` do `crossterm` saíram do `Cargo.toml` do `warden-cli` por não serem mais usadas. Verificado com o mesmo harness de pty, incluindo uma chamada real à API do Gemini de ponta a ponta (não mockada) — turno completo funcionando: caixa de "pensando" por ~10-13s (latência real observada do Gemini nesta sessão), transição limpa pro texto da resposta, contagem de tokens, volta pra caixa de input vazia, tudo sem travar. `cargo build/test/clippy --workspace` limpos. **Detalhado pelo usuário, mesma Sessão 47**: três pedidos concretos — início limpo tipo Claude Code (sem os comandos antigos do shell visíveis atrás do banner), melhorar especificamente "a parte superior" (o banner, texto puro sem cor até então), e responsivo ao tamanho da janela (a caixa de input em si foi elogiada como já boa, não mexida). O item "responsivo" já funcionava — confirmado via pty redimensionando de 120 pra 80 colunas em pleno uso, a borda encolheu certo no próximo frame, sem travar (`ratatui` já revalida o tamanho a cada `draw()` via `ioctl`, não via a query de cursor problemática). Implementados: `clear_screen()` novo (`ESC[2J``ESC[3J``ESC[H` — limpa tela **e** scrollback, home no cursor) chamado antes de tudo em `run()`, o que também some com os avisos de bootstrap ("TAVILY_API_KEY not set" etc.) impressos antes da tela interativa começar; banner redesenhado com a cor de marca roxa que o desktop já usa (`--color-accent` modo escuro, `#a78bfa`, `desktop/src/App.css`) em vez do laranja/amarelo escolhido ao acaso antes só pro CLI, mais uma linha divisória fina do tamanho real do terminal separando o cabeçalho da conversa. Verificado via pty (sem gastar mais cota do Gemini) que a sequência de clear roda antes do banner e as cores aparecem certas; `cargo build/test/clippy --workspace` limpos. **Usuário testou, ainda Sessão 47**: confirmou que era mesmo a cota do Gemini (429), mas achou um bug novo — o texto do erro (JSON de várias linhas) "ficou todo jogado pela tela... e todo picotado". Causa: modo raw desliga o `OPOST` do terminal, que é o que traduz `\n` sozinho pra `\r\n` — todo `println!` chamado depois do raw mode ligado (eco da mensagem, cabeçalho da resposta, linha de tokens, erro) deixava o cursor na coluna onde a linha anterior parou em vez de voltar pra coluna 0; inofensivo numa linha só, mas visível como uma "escada" em qualquer texto de várias linhas — o que inclui respostas normais do modelo com parágrafos, não só erros, só não tinha aparecido ainda porque os testes anteriores usaram respostas de uma linha só ("Hello"). **Corrigido**: dois helpers novos (`write_raw_line`/`write_raw`) trocam `\n` por `\r\n` (inclusive embutido no meio do texto) antes de imprimir; todo print que roda depois do raw mode ligado migrou pra eles. Verificado via pty forçando o mesmo 429 de propósito: zero `\n` sozinho no texto do erro depois do fix. `cargo build/test/clippy --workspace` limpos. **Usuário testou (2026-09-05), achou um bug de layout novo**: "tá tudo cagado, a mensagem está em baixo da caixa de texto, não parece um chat normal" — a caixa de input/status ficava fora de ordem em relação às linhas de conversa, em vez de ficar fixa embaixo com o histórico crescendo acima, como um chat de verdade. **Causa raiz**: `write_raw_line`/`write_raw` (os helpers da correção anterior, mesma Sessão 47) imprimiam a mensagem do usuário, o cabeçalho "● Warden", a resposta e o erro direto via `print!`/`println!` enquanto o modo raw estava ligado — bypassando por completo o rastreamento que o `ratatui` mantém de onde a caixa embutida (`Viewport::Inline`) está na tela. `Terminal::clear()`/`draw()` só sabem redesenhar a caixa na posição que o `ratatui` tem registrada, que só é atualizada por chamadas do próprio `ratatui` (`draw`/`clear`/`insert_before`) — nunca por um `print!` cru. Como a posição real do cursor avançava a cada linha impressa sem o `ratatui` ficar sabendo, a próxima `.clear()`/`.draw()` acertava a linha errada, embaralhando a caixa com o texto já impresso. **Corrigido**: substituído todo `print!`/`println!` em modo raw por `Terminal::insert_before` — a API que o próprio `ratatui` expõe para "imprimir histórico permanente acima de uma caixa embutida fixa", exatamente o caso de uso daqui (visto na doc do crate, `ratatui-core-0.1.2/src/terminal/inline.rs`). `warden-cli/src/interactive.rs` ganhou um wrapper de texto próprio (`wrap_text`/`wrap_segment`, baseado em `unicode_width`, já que `ratatui` só expõe contagem de linhas quebradas via uma API instável) pra saber de antemão quantas linhas cada bloco de histórico vai ocupar, requisito do `insert_before`. A resposta em streaming agora acumula num buffer (`pending`): toda linha terminada em `\n` de verdade é promovida pro histórico assim que completa, e qualquer trecho que já preencheu a largura do terminal (comum — a maioria das respostas é um parágrafo contínuo até a quebra final) também é promovido linha a linha, deixando a caixinha de "Warden respondendo" (nova, `render_streaming_box`, verde, substitui a de "pensando" assim que o primeiro texto chega) sempre com no máximo uma linha de conteúdo — nunca precisa redimensionar. Como bônus, `insert_before` desenha por coordenada de célula no backend, não depende da tradução `\n`→`\r\n` do terminal — o bug anterior de "escada" em texto de várias linhas (mesma Sessão 47) deixa de ser uma preocupação por construção, não só corrigido por um workaround. `cargo build/test/clippy --workspace` limpos (15 testes em `warden-cli`, `LineEditor` intocado). **Verificado via pty** (3 rodadas, incluindo chamadas reais — não mockadas — à API do Gemini): a linha "> mensagem" e um erro real de várias linhas (JSON do Gemini, 503 "high demand") renderizaram corretamente colados acima da caixa, sem sobreposição nem escada, em todas as rodadas — a caixa ficou onde devia (fixa embaixo). **Ainda falta**: o Gemini esteve instável durante a verificação desta sessão (503 "high demand" repetido, e uma chamada que passou de 70s sem responder nem errar) — não foi possível ver uma resposta de texto real (não só erro) renderizando em streaming; a lógica de promoção linha-a-linha do `pending` ficou coberta só pelo raciocínio + os testes automatizados, não por uma resposta de verdade na tela. Vale o usuário confirmar com uma mensagem simples assim que o Gemini normalizar. **Usuário testou (ainda 2026-09-05), confirmou melhora mas pediu mais**: "melhorou mas ainda não está um claude code da vida". Detalhado (múltipla escolha): falta cor/formatação (markdown cru), visual das mensagens sem identidade clara, caixa de pensando/status ainda incomoda, "como as mensagens ficam estruturadas, tá mt esquisito". **Implementado**: markdown inline (`parse_inline` — negrito/itálico/código, não-CommonMark-completo) + classificação de bloco (`classify_and_strip` — cabeçalho/lista/bloco de código cercado) + wrap preservando estilo (`wrap_spans`) + estrutura de "hanging indent" por mensagem (`insert_history_rows`/`commit_assistant_rows` — só a primeira linha de cada mensagem leva o marcador "> "/"● ", as demais um recuo de 2 espaços, pra virar um bloco visualmente coeso em vez de linhas soltas) + eco do usuário mudou de negrito laranja pra "> " discreto/dim + caixa de pensando/status perdeu a borda (virou uma linha solta, só o input continua com `Block` — o único que o usuário já tinha elogiado antes). `cargo build/test/clippy --workspace` limpos (21 testes no crate, 6 novos). **Verificado via pty com uma resposta REAL do Gemini** (não só erro, dessa vez): "● " verde só na 1ª linha, "• " roxo em cada bullet, negrito de verdade nas palavras marcadas, continuação recuada sem repetir marcador, eco discreto, status sem borda — tudo conferido por dump de tela + dump de estilo por célula. **Limitação conhecida e aceita**: como a maior parte do texto ainda precisa ser promovida pro histórico ANTES da linha terminar de verdade com `\n` (senão fica invisível até o fim — o problema que a correção anterior resolveu), a classificação de bloco só roda na linha já completa; o trecho ainda em streaming usa só parsing inline sem marcador de bloco — um marcador de markdown que calhe de ficar dividido bem na fronteira entre duas promoções separadas pode deixar um caractere solto (ex. um asterisco) visível. Confirmado acontecendo uma vez no teste real desta sessão (efeito raro, cosmético, não quebra o layout) — aceito deliberadamente dado o trade-off. **Ainda falta**: confirmação visual do próprio usuário numa janela real. **Usuário testou de novo (ainda 2026-09-05, mesma Sessão 48)**: confirmou que o layout (estrutura, cor, caixa sem borda) melhorou, mas pediu mais: "ta simples dms, não tem como deixar mais bonitão, com mais detalhes e tudo mais?". Apresentadas 3 opções visuais (mockups em ASCII) — usuário escolheu **cartões com borda por mensagem** (cada mensagem, sua e do Warden, numa caixinha própria com borda arredondada, tipo card de app de chat), em vez de manter o visual solto com "hanging indent" ou um meio-termo só com a resposta em caixa. **Continuação (ainda Sessão 48)**: usuário notou que mensagens curtas ("oi", "ok") ficavam num cartão esticado até a borda do terminal igual um de resposta longa, e pediu pra encolher o cartão pro tamanho da mensagem. **Corrigido**: largura de `insert_card` extraída pra função pura `card_width(title_width, content_rows, max_width)` — usa a linha mais longa do título/conteúdo, piso de 12 colunas, teto na largura do terminal; como o corpo já vem pré-quebrado pelo teto do terminal, encolher depois nunca força re-quebra. 2 testes novos (23 no crate), `cargo build/test/clippy --workspace` limpos. Verificado via pty com harness próprio (mini-emulador VT em Python, não commitado — `pyte` não estava disponível e o ambiente não tem `pip` sem root): "oi" virou cartão de ~12 colunas ao lado de um cartão de erro real (429 do Gemini) continuando esticado até a borda, lado a lado. **Ainda em aberto**: segue faltando ver uma resposta completa (não-erro) no formato de cartão, por causa do rate limit do Gemini.

**Reescrito pra cartões** (`warden-cli/src/interactive.rs`): `insert_history_rows`/`commit_assistant_rows` (da rodada anterior) removidos — substituídos por `insert_card` (desenha um cartão completo: borda de cima com título embutido, linhas de conteúdo já quebradas/estilizadas emolduradas por "│ "/" │", rodapé opcional com espaçador antes, borda de baixo — tudo numa única chamada de `insert_before`, já que uma borda não pode ser "reaberta" depois de fechada pra receber mais linhas). Isso mudou a estratégia de streaming: como um cartão só pode ser commitado uma vez por inteiro, **a resposta parada de ser promovida linha a linha durante o streaming** (o que resolve de vez a limitação conhecida da rodada anterior — marcador de markdown dividido entre duas promoções) — em vez disso, o texto cru vai só acumulando (`pending`) e é mostrado ao vivo numa pré-visualização com borda (`render_card_preview`, mesma borda arredondada do cartão final, título "● Warden") que **cresce dinamicamente** conforme a resposta cresce (até um teto de `MAX_PREVIEW_ROWS = 6` linhas, depois disso só mostra a cauda) — isso exigiu voltar a reconstruir o `Terminal` em tempo de execução (`ensure_preview_height`/`new_inline_terminal_with_height`), a mesma operação que causou o travamento da Sessão 47 com o `EventStream` antigo — mas agora é seguro, porque o `EventStream` já foi removido de vez naquela sessão (só `poll`/`read` síncronos limitados restam), então reconstruir o `Terminal` só faz uma query de cursor pontual sem risco de lock preso. Só quando a resposta termina de verdade é que o texto completo passa pelo parser de markdown (`markdown_body_rows`) de uma vez só e vira um cartão permanente — sem mais chance de um `**`/`` ` `` ficar cortado ao meio. Mensagem do usuário também virou cartão (`plain_body_rows`, sem parsing de markdown — é texto literal dele), título "você" discreto/dim, borda dim; erro também virou cartão, com borda vermelha. A caixa de input (`render_input_box`) ganhou borda arredondada (`BorderType::Rounded`) pra combinar visualmente com os cartões novos — antes usava o canto reto padrão do `ratatui`.

`cargo build/test/clippy --workspace` limpos (mesmos 21 testes, nenhum quebrou — a suíte cobre as funções puras de parsing/wrap que não mudaram, `insert_card`/`ensure_preview_height` não são testáveis sem terminal de verdade). **Verificado via pty com o mesmo harness**: cartão do usuário e caixa de pré-visualização renderizando corretamente com borda arredondada; bateu num rate limit do Gemini de novo (429, cota por minuto dessa vez, `retryDelay: 57s`, não a cota diária) — o cartão de ERRO em si renderizou certinho (borda vermelha, texto JSON de várias linhas emoldurado e alinhado, cada linha com "│" nos dois lados na coluna certa) mas o JSON do erro foi grande o bastante (~30 linhas) pra estourar a altura do terminal de teste (34 linhas) e rolar o título/borda de cima pra fora da tela capturada — artefato do teste (terminal pequeno), não um bug, já que o mecanismo de desenho da borda de cima é idêntico ao que já foi confirmado funcionando na rodada anterior. **Ainda falta**: nunca visto uma resposta de verdade (não só erro) completa no novo formato de cartão — toda vez que tentou hoje bateu em rate limit do Gemini antes de completar; usuário pediu pra deixar assim por enquanto e seguir depois | `PENDING.md` P8 (Sessão 46/47/48) | 🟠 Média |
| P35 | ~~**App Mobile — troca de stack: Tauri Mobile revertido, Flutter decidido (Fase 7.1 recomeça do zero)**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo (iOS segue sem teste, registrado como P39) | Sessão 50 (continuação) |
| P36 | **`warden-server` (Fase 9.2) sem TLS próprio + chave de auth sem rotação/UI** — o novo crate `crates/warden-server` implementa o handshake/heartbeat do protocolo servidor↔cliente (P1) rodando sobre `ws://` puro (sem TLS/criptografia própria) — depende inteiramente do túnel da Fase 9.1 (Tailscale, ainda não implementada) pra criptografia de transporte; rodar `--listen` numa interface fora da tailnet antes da 9.1 existir mandaria a chave de auth em texto puro pela rede (mitigado só parcialmente por exigir a chave, não por escondê-la em trânsito). A chave de auth em si é só um valor plano (`--auth-key`/`WARDEN_SERVER_AUTH_KEY`), sem rotação, sem UI de gerenciamento, sem registro de "quem tem essa chave" — fica pra quando a 9.3 (registro/pareamento de cliente) for implementada. **Também sem teste sobre uma tailnet real** — só testado sobre `127.0.0.1` (`crates/warden-server/tests/handshake.rs`), sem infra de Tailscale disponível neste ambiente de dev, mesma lacuna já aceita em outras partes do projeto por falta de infra externa (ex. P29/P30/P31). **Atualizado 2026-09-06 (Sessão 50, continuação — 7.2)**: a mesma lacuna se estende agora ao cliente mobile Flutter — verificado de ponta a ponta contra um `warden-server` real, mas só via `ws://10.0.2.2:7420` (emulador Android → host), nunca sobre uma tailnet de verdade. `android:usesCleartextTraffic="true"`/`NSAllowsArbitraryLoads` no lado Flutter tornam isso explícito (aceitam `ws://` sem TLS pra qualquer host, não só o de dev). **Atualizado 2026-09-06 (Sessão 51 — 7.3)**: a mesma lacuna cobre agora o tráfego de `Chat`/`ChatResponse` também (não só handshake/heartbeat) — verificado de ponta a ponta com o Gemini real do usuário, mas ainda só via `10.0.2.2`, nunca sobre uma tailnet de verdade; uma resposta de modelo passa pela rede em texto puro até a 9.1 existir | `PHASE.md` Fase 9.2/7.2/7.3, `ARCHITECTURE.md` (Sessão 50/51) | 🟡 Baixa |
| P37 | ~~**Direção nova: TruthID como pagador/publicador Arweave pro vault+config do Warden (substitui P24)**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 54 (2026-09-06) |
| P38 | **`warden-truthid` (Sessão 50) nunca testado contra o app TruthID real** — a implementação (protocolo, cifra de duas fases, varredura de LAN) foi verificada com um conjunto forte de testes automatizados: vetor conhecido de RFC 5869 pra HKDF, round-trip de cada camada de cifra, e um fluxo de ponta a ponta completo contra um "celular fake" (servidor `axum` de teste que fala exatamente o mesmo protocolo HTTP) — mas **nunca contra o app TruthID de verdade rodando num celular real**, por pedido explícito do usuário nesta sessão ("não quero hardware agora"). Maior risco de interoperação registrado: a convenção exata do "ECDH secret" do pacote Dart `elliptic` (assumido como a coordenada X crua do ponto compartilhado, mesma convenção do `k256`, mas não confirmada rodando o código Dart de verdade). **Atualizado 2026-09-10 (Sessão 59, continuação 3)**: esse maior risco foi fechado com evidência real (o resto do item continua em aberto). Não existe SDK oficial do TruthID em Rust (só Dart/Python/Ruby/TypeScript, conferido em `~/Documents/workspace/truthid/sdk/`) — pedido do usuário foi usar o SDK Dart real como oráculo de validação do `warden-truthid`, sem virar dependência de runtime (que continua sendo a implementação Rust). Dart SDK instalado (`pacman -S dart`, sem precisar do Flutter inteiro), dependências resolvidas (`dart pub get`), e um script descartável (escrito, rodado, **apagado** — `git status` no repo do TruthID confirmado limpo depois) executou o código real de `elliptic-0.3.12/lib/src/ecdh.dart` (`computeSecret`) e de `sdk/dart/lib/src/internal/{hkdf,pin_content_cipher}.dart` contra entradas fixas (chaves privadas `1`/`2`, sessionId fixo). **Resultado: convenção idêntica** — `computeSecret` é a coordenada X do ponto, big-endian, zero-padded a 32 bytes, exatamente o que `k256::ecdh::diffie_hellman(...).raw_secret_bytes()` já produzia; a derivação HKDF do `pin_content_key` também bateu byte-a-byte. 2 testes novos em `crates/warden-truthid/src/crypto.rs` (`matches_the_real_dart_sdk_pin_content_key_vector`, `ecdh_shared_secret_matches_the_real_dart_sdk_elliptic_package`) travam esses vetores reais como regressão permanente — 11 testes de `warden-truthid` passando, `clippy` limpo. **Continua em aberto**: o fluxo de ponta a ponta contra o app TruthID de verdade rodando num celular físico segue sem teste | `PENDING.md` P37, `ARCHITECTURE.md` (Sessão 50) | 🟡 Baixa |
| P24 | **Fase 4 (Vault & Memória) precisa repensar IPFS → Arweave** — o `PHASE.md` (Fase 4) e `OVERVIEW.md` ainda descrevem espelho em IPFS (Filebase/Pinata). Usuário sinalizou (2026-08-29) que o **TruthID** — projeto irmão do mesmo ecossistema — já migrou de IPFS para **Arweave** (confirmado nesta sessão: `~/Documents/workspace/truthid/docs/docs/sdk/dart.md` usa carteira Arweave por identidade, publica bytes pagos pela própria carteira, e `cid` no SDK é hoje um ponteiro `ar://<tx_id>`, não mais um CID IPFS). Antes de desenhar a Fase 4 do Warden, vale estudar a arquitetura de vault do TruthID a fundo (que decisões de custo/persistência/pinning o Arweave implica, diferente do modelo de pinning do IPFS). **Atualizado 2026-09-06 (Sessão 50)**: esse estudo profundo foi feito de verdade (código real, não só docs) e virou P37, que carrega a direção concreta daqui pra frente (TruthID como pagador via `pin()`, tudo cifrado, primeira peça — `warden-truthid` — já implementada) — este item fica só como o registro histórico de quando a dúvida surgiu | `ROADMAP.md`, pedido do usuário 2026-08-29 | 🟡 Baixa (ver P37) |
| P39 | **App Mobile em Flutter (P35/7.1) — lado iOS nunca testado** — a 7.1 refeita em Flutter (Sessão 50, continuação) foi verificada de ponta a ponta só no lado **Android** (build real + instalação + abertura no emulador `warden_test` + screenshot via `adb`, mesmo rigor da tentativa anterior em Tauri Mobile). `flutter create` já gerou o projeto Xcode em `mobile/ios/` (plataforma `ios` incluída desde o início), mas **nada disso foi testado nem construído** — exige Xcode, que só roda em macOS, indisponível neste container Linux. Mesma lacuna que já existia na tentativa anterior com Tauri Mobile (era o motivo P24/original desta troca), só que agora carregada pro framework novo | `PENDING.md` P35, `ARCHITECTURE.md` (Sessão 50) | 🟡 Baixa |
| P40 | **Chat mobile (7.3) sem fetch de histórico ao reconectar** — o protocolo `warden-server`↔cliente não tem uma mensagem de "buscar histórico"; `warden-server` persiste a conversa em disco por `device_id` (mesmo `handle_turn` do Telegram/WhatsApp), mas `ChatScreen` no Flutter só guarda mensagens em memória pro tempo de vida da tela — fechar o app, reconectar, ou só sair e voltar da tela (P41) começa com a transcrição vazia mesmo com histórico real salvo no servidor. Escopo estreito deliberado da 7.3 (mesmo espírito da 7.2); resolver exigiria uma mensagem nova de protocolo tipo `RequestHistory`/`History{messages}` | `ARCHITECTURE.md` (Sessão 51, seção "7.3") | 🟡 Baixa |
| P41 | **Mobile: voltar da `ChatScreen` sem querer desconectar não tem caminho de volta pela UI** — `ChatScreen` deixa a conexão viva ao navegar pra trás (só o botão explícito "Disconnect" desliga), o que é o comportamento certo, mas `ConnectionScreen` só sabe mostrar "Connect" (desconectado) ou "Disconnect" (conectado) — não existe um terceiro estado "conectado, retomar o chat", então o único jeito de voltar pra `ChatScreen` depois de sair dela sem querer é desconectar e reconectar de novo (o que funciona, mas perde o histórico em memória — ver P40 também). Achado durante a verificação manual da 7.3, aceito como lacuna pequena, fora do escopo aprovado daquela sessão | `ARCHITECTURE.md` (Sessão 51, seção "7.3") | 🟡 Baixa |
| P42 | **Colisão de nome de tool entre um cliente remoto e o `Orchestrator` compartilhado não é detectada** — achado de verdade na 7.4 (Sessão 52): a primeira tentativa nomeou as tools do celular `list_files`/`read_file`, os mesmos nomes já usados por `ReadFileTool`/`WriteFileTool` (vault) — a API do Gemini rejeitou a chamada inteira com 400 "Duplicate function declaration found". Corrigido renomeando as tools do celular (`list_phone_files`/`read_phone_file`), mas `server.rs` não valida isso — registrar um `RemoteTool` cujo nome já existe no `Orchestrator` base simplesmente quebra a próxima chamada de API do provider, sem erro claro do lado do Warden (o erro que aparece é o 400 cru do provider, só entendível lendo `ARCHITECTURE.md`). Vale validar no `Hello` (recusar ou avisar sobre um nome duplicado) quando um segundo cliente com tools reais existir — só um caso hoje (mobile), não urgente | `ARCHITECTURE.md` (Sessão 52, seção "7.4") | 🟡 Baixa |
| P43 | **`write_phone_file` (escrita no celular) deliberadamente fora do escopo da 7.4** — decisão explícita do usuário antes de planejar: só leitura por enquanto (`list_phone_files`/`read_phone_file`), já que deixar o modelo escrever arquivos arbitrários no celular do usuário é um risco maior que ler, mesmo escopado a uma pasta só. Adicionar espelharia `WriteFileTool` (vault) usando `saf_stream`'s `pasteLocalFile`/operações de escrita, se algum dia fizer falta | `ARCHITECTURE.md` (Sessão 52, seção "7.4"), pedido do usuário 2026-09-06 | 🟡 Baixa |
| P44 | **Tool local no mobile (7.4) é Android-only, sem equivalente iOS** — o par `saf_util`/`saf_stream` (Storage Access Framework) não existe pro iOS, que usa um modelo de arquivos completamente diferente (document picker + security-scoped bookmarks). Mesma lacuna geral de "iOS nunca testado" (P39), agora estendida a esta capacidade específica — sem Xcode/macOS neste ambiente pra sequer avaliar o esforço de portar | `ARCHITECTURE.md` (Sessão 52, seção "7.4"), `PENDING.md` P39 | 🟡 Baixa |
| P45 | ~~**Um agente por conversa, escolhido no início**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 57 (2026-09-08) |
| P46 | **Orquestração de agentes — dois modos, não um só** — evolui a ideia de "sub-agentes autônomos" já no `ROADMAP.md`: usuário quer suportar tanto (a) **modo centralizado**, um agente "chefe" único que cria/comanda outros agentes por trás, o usuário só fala com o chefe, quanto (b) **modo "funcionários"**, vários agentes especializados e independentes, sem nenhum agente global coordenando, o usuário fala com cada um diretamente. Em ambos, agentes devem poder criar outros agentes (não só um orquestrador raiz fixo). Arquitetura pra suportar os dois modos ao mesmo tempo ainda em aberto. **Atualizado 2026-09-08 (Sessão 57)**: escopo fechado com o usuário antes de codar — começar só pelo **núcleo técnico** (delegação recursiva com profundidade limitada), sem UI nem fila de jobs ainda. Implementado: `DelegateTool` deixou de proibir um sub-orchestrator com outro `DelegateTool` registrado; `warden_bootstrap::build_delegating_orchestrator` monta uma cadeia recursiva de até `DELEGATE_MAX_DEPTH = 2` níveis, parada estrutural (o nível terminal nunca anuncia `delegate_task`, não uma checagem em runtime). Ver `ARCHITECTURE.md` pros detalhes técnicos. **Atualizado 2026-09-08 (Sessão 57, continuação — profundidade configurável)**: `FileConfig.delegate_max_depth: Option<u32>` novo, `WARDEN_DELEGATE_MAX_DEPTH` vencendo por cima (`resolve_delegate_max_depth`, mesmo padrão env-vence-arquivo de `resolve_flag`), caindo pro default `2` (renomeado `DEFAULT_DELEGATE_MAX_DEPTH`) quando nada configurado. Deliberadamente sem UI (Settings do desktop só carrega o valor existente adiante, `existing.delegate_max_depth`, mesmo tratamento de `telegram_bot_token`) e sem teto/clamp — aumentar o valor é escolha explícita do usuário de aceitar um `MAX_TOOL_ITERATIONS ^ depth` maior (ver P60). `cargo test -p warden-bootstrap` (36 testes) e `cargo clippy --workspace --all-targets` limpos. **Atualizado 2026-09-08 (Sessão 57, continuação — `delegate_to_agent`)**: mecanismo concreto do modo centralizado entregue — tool nova `delegate_to_agent` (`crates/warden-core/src/tool/delegate_to_agent.rs`) endereça um agente **configurado** específico por id (persona/provider próprios), diferente de `delegate_task` (sub-agente anônimo). Opt-in por agente confirmado com o usuário: `AgentConfig.can_delegate_to_agents: bool` novo — só quem tem a flag ligada ganha a tool. Descoberta em planejamento que mudou o design: como existe um único `Orchestrator` compartilhado (tools fixas desde `bootstrap()`, só persona/modelo trocam por turno), o opt-in de verdade exigiu `Orchestrator::with_tool` novo e que desktop (`send_message`) e CLI (`resolve_turn_context`/`run_turn`) — os únicos dois canais que já resolvem `agent_id` por turno — conferissem a flag e anexassem a tool condicionalmente; `warden_bootstrap::build_delegate_to_agent_tool(config, orchestrator)` monta os alvos a partir de `config.agents`. Ver `ARCHITECTURE.md` pra a limitação aceita (alvo nunca vira chefe recursivamente) e os 4 pontos de `AgentConfig` ajustados pra não perder a flag hand-edited a cada save. `cargo test -p warden-core -p warden-bootstrap -p warden-cli` (70+36+34+5 testes) e `cargo clippy --workspace --all-targets`/`npm run build` limpos. **Atualizado 2026-09-08 (Sessão 57, continuação 8 — UI/CLI pra `can_delegate_to_agents`)**: a última lacuna de superfície fechada — a flag agora é ligável em ambos os canais que já resolvem `agent_id`, sem precisar de hand-edit no `config.toml`. Desktop: `AgentPayload`/`AgentEntry` ganharam `can_delegate_to_agents`/`canDelegateToAgents`; `save_settings` parou de sempre carregar o valor antigo de `existing.agents` (única fonte antes) e passou a aceitar o valor vindo do payload do form — o "carry forward" que existia era, na prática, o que impedia a UI de ligar a flag. `AgentCard` (`SettingsView.tsx`) ganhou um checkbox "Can delegate to other agents", mesmo padrão visual do checkbox de OAuth do MCP (`settings-checkbox-field`/`settings-checkbox-row`). CLI: novo prompt `prompt_agent_can_delegate` (s/n) no wizard `wizard_agents_create`/`wizard_agents_edit`; `/agents` (list) ganhou o marcador `[delega]` ao lado do agente que tem a flag ligada. `cargo test -p warden-core -p warden-bootstrap -p warden-cli` e `cargo clippy --workspace --all-targets` limpos; `tsc`/`npm run build` limpos. **Verificado via Playwright headless contra o dev server real** (mesmo padrão da Sessão 49/Usage): `get_settings`/`save_settings` mockados via `addInitScript`, checkbox encontrado e marcável, payload salvo confirmado com `canDelegateToAgents: true`, zero erro de console. **O que segue em aberto**: fila de jobs, controle de custo por sub-agente, isolamento de tools por sub-agente (ver P60 pro risco de custo aceito) | `ROADMAP.md` ("Sub-agentes autônomos"), `PENDING.md` P8/P60, pedido do usuário 2026-09-06, núcleo implementado 2026-09-08, profundidade configurável 2026-09-08, delegate_to_agent 2026-09-08, UI/CLI 2026-09-08 | 🟠 Média |
| P47 | **SSH — conectar com VPS/máquinas externas** — cadastrar chaves SSH nas configurações do Warden, dar (ou negar) à IA acesso a cada uma, IA usar isso pra rodar comandos em servidores remotos (não só a máquina local, que já tem a tool `shell` da Fase 5.5). Usuário também quer que a IA possa **criar** esse tipo de infraestrutura — escopo exato (provisionar VPS? só conectar em VPS já existente?) não detalhado ainda | `ROADMAP.md`, pedido do usuário 2026-09-06 | 🟡 Baixa |
| P48 | **Avatares/personas 3D pros agentes** — dar um personagem visual a um agente (ex. um gatinho), gerado em 3D com animações, criável via (a) só prompt de texto ou (b) foto + prompt (ou só foto). Avatar poderia "se mexer no computador" — overlay animado na tela, não só ícone estático. Sem arquitetura definida — geração 3D a partir de texto/imagem e rigging de animação automático são problemas de pesquisa em si, nenhum modelo/serviço escolhido ainda | `ROADMAP.md`, pedido do usuário 2026-09-06 | 🟡 Baixa |
| P53 | ~~**App Mobile (Flutter) sem vault local nem participação no sync (P37)**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo (push/pull real e iOS seguem sem teste, ver P38/P55/P39/P44) | Sessão 55 (2026-09-07) |
| P54 | **Credenciais OAuth de MCP não fazem parte do sync (P37)** — `~/.config/warden/mcp_oauth/{name}.json` (um arquivo por server MCP autenticado via OAuth, ver P26) fica fora do escopo do `crates/warden-sync` v1: é um conjunto de arquivos dinâmico e não-limitado (cresce a cada server OAuth novo), exigiria um sub-manifesto próprio pra rastrear. Um device que pareia e puxa o vault/config precisa reconectar manualmente cada server OAuth (botão "Connect" da Settings) | `PENDING.md` P37, `ARCHITECTURE.md` (Sessão 54) | 🟡 Baixa |
| P55 | **`crates/warden-sync` (Sessão 54) nunca testado contra o app TruthID real nem contra o Arweave de produção** — toda a verificação (motor de sync, protocolo de pareamento, cliente GraphQL) foi feita com testes automatizados usando um "telefone" e um "gateway Arweave" falsos (mesmo padrão já aceito em `warden-truthid`, ver P38) — nunca contra hardware real nem a rede Arweave de verdade. Maior risco: a query GraphQL exata (`transactions(owners:...)`) precisa bater com o schema real do gateway `arweave.net`, só verificado contra documentação, não contra uma resposta real | `PENDING.md` P37/P38, `ARCHITECTURE.md` (Sessão 54) | 🟡 Baixa |
| P49 | **Overlay "Super Jarvis" — atalho global + avatar na tela** — evolução do app "Copilot" (P9): atalho de teclado global ativa áudio ou abre uma tela de busca/chat da IA sem precisar abrir o app; nessa tela o avatar (P48) aparece, dá pra conversar por texto ou voz. Visão declarada pelo usuário como "um super Jarvis mesmo" | `ROADMAP.md`, `PENDING.md` P9, pedido do usuário 2026-09-06 | 🟡 Baixa |
| P50 | **Tier pago — hospedagem do servidor pelo próprio Warden** — primeira menção de um modelo de negócio pago no projeto. Hoje a Fase 9 (rede de nós/servidor) pressupõe o usuário hospedando o próprio servidor (VPS ou em casa); um tier pago ofereceria hospedar esse servidor pelo próprio Warden (SaaS), pra quem não quiser cuidar de infraestrutura própria | `ROADMAP.md`, `PHASE.md` Fase 9, pedido do usuário 2026-09-06 | 🟡 Baixa |
| P51 | **"9Router" — API do agente pessoal + OAuth de contas de IA** — evolução concreta de "Warden API" (P12): API completa expondo o agente pessoal do usuário (conhecimento do vault + personalidade configurada) pra usar em **outros harnesses**, não só dentro do Warden. Inclui conectar contas via **OAuth de provedores de IA** (Claude, GPT, outros) — presumivelmente pra usar a conta/assinatura que o usuário já paga em vez de (ou além de) uma chave de API bruta. Usuário sinalizou que "vem a hora" de puxar isso pra frente | `ROADMAP.md`, `PENDING.md` P12/P10, pedido do usuário 2026-09-06 | 🟠 Média |
| P52 | ~~**Estrutura padrão do vault + visualização pela interface**~~ | ✅ Resolvida — ver seção "Resolvidas" abaixo | Sessão 57 (2026-09-08) |
| P56 | **Busca semântica (Fase 4.5/P6) — caminho de fallback pro grep nunca exercido de verdade** — `Orchestrator::handle_turn_streaming` tenta `Vault::search_semantic` e cai pro `Vault::search` (grep) em qualquer erro (ex. sem rede no primeiro download do modelo `fastembed`), mas essa sessão só verificou o caminho feliz (rede disponível, download e ranking reais funcionando) — o próprio fallback nunca foi exercitado contra uma falha real (rede bloqueada de propósito, ou modelo corrompido). Risco baixo (o código do fallback é simples, um `.unwrap_or_else` puro), mas fica registrado por não ter sido visto rodando de verdade | `PENDING.md` P6, `ARCHITECTURE.md` (Sessão 56) | 🟡 Baixa |
| P57 | **Notificações locais (Fase 7.5) são Android-only, sem equivalente iOS** — `mobile/lib/services/chat_notifications.dart` usa `AndroidFlutterLocalNotificationsPlugin` explicitamente (`resolvePlatformSpecificImplementation`) pra pedir a permissão de notificação, e `showChatNotification` só monta `NotificationDetails(android: ...)` — sem o campo `iOS:`, uma notificação nunca apareceria no iOS mesmo que o resto do app rodasse lá. Mesma lacuna geral de "iOS nunca testado" (P39/P44), estendida a mais uma capacidade — sem Xcode/macOS neste ambiente pra sequer avaliar o esforço de portar (`DarwinInitializationSettings`/`DarwinFlutterLocalNotificationsPlugin` seriam o equivalente, não implementado) | `PENDING.md` P39/P44, `ARCHITECTURE.md` (Sessão 56, seção "Fase 7.5") | 🟡 Baixa |
| P59 | **App Mobile — cores de marca (P58) nunca vistas numa janela real** — o `ColorScheme.fromSeed` novo (`mobile/lib/main.dart`, tokens `#7c3aed`/`#a78bfa` do desktop) foi verificado só com `flutter analyze`/`flutter test` (33 testes, nenhum novo — mudança é só configuração visual, sem lógica testável) — nunca visto rodando de verdade num dispositivo/emulador. Usuário pediu explicitamente pra fazer a troca de cor sem esperar o teste no celular, deixando essa verificação como pendência separada. Falta: abrir o app (emulador ou celular real) e confirmar visualmente que os tons de roxo da marca aparecem nas telas Connection/Chat/Sync, em claro e escuro (`ThemeMode.system`) | `PENDING.md` P58 (resolvida), pedido do usuário 2026-09-08 | 🟡 Baixa |
| P60 | **Delegação recursiva (P46, núcleo) — sem teste de ponta a ponta com modelo real, e sem controle de custo pra limitar o pior caso** — `build_delegating_orchestrator`/`DEFAULT_DELEGATE_MAX_DEPTH` (`warden-bootstrap`) foram verificados só com um teste unitário determinístico (mock roteirizado por ordem de chamada, ver `ARCHITECTURE.md`) — nunca visto um modelo real decidindo delegar de propósito mais de um nível (mesma limitação que a v1 do `DelegateTool`, Sessão 11, já tinha aceitado — forçar isso de propósito contra uma API real não é confiável de scriptar). Risco separado, mais concreto: sem fila de jobs/controle de custo (P4), o pior caso é `MAX_TOOL_ITERATIONS ^ depth` chamadas de modelo (64 na profundidade default de 2) se todo mundo delegar em toda iteração — improvável na prática, mas nada no código impede, e nenhuma sessão testou esse cenário adversarial de propósito. **Atualizado 2026-09-08 (Sessão 57, continuação)**: a profundidade virou configurável (`delegate_max_depth` em `config.toml`/`WARDEN_DELEGATE_MAX_DEPTH`, sem UI, sem clamp de teto) — o risco não foi eliminado, só ficou **maior e sob controle explícito do usuário**: quem aumentar o valor está aceitando de propósito um pior caso maior que `64`, sem nenhum aviso/validação no código pra isso | `PENDING.md` P46/P4, `ARCHITECTURE.md` (Sessão 57) | 🟡 Baixa |
| P62 | **"Agent Builder" — agentes que se criam e se capacitam sozinhos por linguagem natural** — visão grande trazida pronta pelo usuário em documento próprio (2026-09-08, `JARVIS_Agentes_Autocapacitacao.md`, apagado da raiz e incorporado aqui/em `ROADMAP.md`). Evolui o Warden de "agente geral" pra plataforma capaz de **criar, capacitar, avaliar, atualizar e orquestrar agentes especializados** a partir de uma frase (ex. "crie um agente especialista em servidores Linux, bom em administração/diagnóstico/segurança") — sem o usuário escrever prompt, importar documentação ou montar base de conhecimento manualmente. Fluxo proposto (iterativo, não geração única): especificar (área/objetivos/conhecimentos/ferramentas/riscos/critérios de competência a partir da descrição) → pesquisar fontes com **níveis de confiabilidade explícitos** (alta: docs oficiais/specs/RFCs/acadêmico; média: livros/artigos técnicos/repos relevantes; baixa: fóruns/posts sem autoria — úteis pra achar problemas práticos, mas sem a mesma confiança) → construir base de conhecimento **rastreável até a fonte** (permite auditoria, detectar conflito, priorizar fonte melhor) → descobrir **ferramentas** necessárias (não só o que o agente precisa *saber*, também o que precisa *conseguir fazer*) → gerar **testes de competência** representativos do domínio → avaliar (precisão, qualidade de decisão, uso correto de tools, segurança, reconhecer incerteza, consistência) → achar lacuna → pesquisar de novo → reavaliar → liberar. Depois de criado, capacitação **contínua** (novas versões/vulnerabilidades/feedback do usuário/erros do próprio agente atualizam a base sem reconstruir tudo), com um ciclo de autocorreção explícito: Executar → Avaliar → Encontrar lacuna → Pesquisar → Melhorar → Testar de novo. Separa também **conhecimento do domínio** de **conhecimento do ambiente específico** (ex. um agente Linux aprendendo que este ambiente usa Arch, Docker, tal estrutura de storage) — os dois se combinam com o contexto da tarefa. Princípio de segurança central: **inteligência do agente ≠ autoridade do agente** — o agente decide que precisa rodar algo, o sistema de permissões decide se pode, com confirmação do usuário em ações de risco. Propõe um **registro central de agentes** (nome, especialidade, nível de competência, fontes, ferramentas, permissões, histórico de avaliação, última atualização) pro JARVIS/Warden principal **descobrir automaticamente** qual especialista usar — resolver sozinho, usar um especialista, usar vários e combinar resultados, ou criar um novo — evitando multi-agente desnecessário quando um único agente já resolve (complexidade/custo/latência). O documento pede explicitamente que isso seja tratado como **extensão do que já existe no Warden**, não projeto separado: antes de implementar, mapear o que já existe (`DelegateTool`/P46, agentes nomeados em `config.toml`, MCP como fonte de ferramentas, o vault como base de conhecimento) / precisa adaptar / precisa criar do zero, priorizando reuso sobre reconstrução. Relaciona-se diretamente com "Sub-agentes autônomos" (P8) e "Orquestração de agentes" (P46, núcleo técnico já implementado) já registrados — esta ideia é bem mais ampla que os dois (cobre pesquisa autônoma, avaliação e auto-capacitação, não só delegação/orquestração). **Nada disso foi levado a `/plan` ainda** — passos sugeridos pelo próprio usuário no documento original: revisar doc atual do Warden, mapear reuso, definir fluxo mínimo de criação de agente, prototipar um Agent Builder, implementar pesquisa automática de conhecimento, criar um primeiro agente especialista como prova de conceito, implementar avaliação automática, implementar ciclo de melhoria, integrar ao Warden principal | `JARVIS_Agentes_Autocapacitacao.md` (trazido pronto pelo usuário, apagado da raiz), `ROADMAP.md`, `PENDING.md` P8/P46, pedido do usuário 2026-09-08 | 🟡 Baixa |
| P63 | **Sync via remote git próprio (Gitea/GitHub self-hosted) como alternativa ao Arweave/TruthID — motor irmão de `warden-sync`** — proposta trazida pelo usuário nesta conversa (2026-09-10): quem não quiser depender de infra paga/Web3 (Arweave via TruthID, P37) deveria poder apontar o Warden pra um remote git próprio (ex. um Gitea self-hosted) como backend alternativo de sync, com a mesma exigência inegociável de tudo cifrado no cliente antes de sair da máquina (mesma postura do Arweave, ver linha "Sync descentralizado" em `ARCHITECTURE.md`). Motivação dupla: (1) autonomia de infra (não depender de Arweave/carteira do TruthID pra sincronizar memória), e (2) um bug real de design encontrado revendo `pull.rs`: hoje `pull()` busca só a **última** tx via `arweave.latest_tx_by_owner` (GraphQL) e aplica só aquele diff — um device pareado que nunca sincronizou antes recebe só o diff da última publicação, não o vault inteiro reconstruído (o pareamento, `pairing/`, propaga só `vault_key`+`owner_address`, nunca `last_tx_id`, de propósito — ver P37/Sessão 54 — então a expectativa implícita de hoje é a base já ser comum, o que nem sempre é verdade). **Decisão confirmada nesta sessão (sem código, só arquitetura)**: git entra como um **motor irmão de `warden-sync`**, não como uma nova implementação de `StorageProvider` (mesmo raciocínio já fechado em P61 — trait deliberadamente "burra"/CRUD-síncrono por arquivo, incompatível com publicar um bundle versionado + resolver "qual é a última versão"); um novo `git.rs` reaproveita `bundle.rs`/`diff.rs`/`manifest.rs`/`pairing/` **inalterados**, só troca a camada de transporte/descoberta que hoje é `arweave.rs`. **Modelo de dados escolhido**: continuar com o `SyncBundle` como um blob único cifrado por push (não uma árvore de arquivos cifrados individualmente espelhando o vault) — a alternativa de árvore por nota foi considerada porque `git pull`/`clone` materializa o estado completo de graça, mas foi descartada por vazar estrutura/nomenclatura do vault (ex. `notas/terapia/2026-09-10.md`) pro operador do servidor git mesmo com conteúdo cifrado, exigindo um esquema extra de ofuscação de nome + índice cifrado à parte só pra não reabrir esse vazamento — complexidade não justificada ainda. Em vez disso, **o bug do device novo é resolvido no `pull`, não no modelo de dados**: cada push vira um commit carregando o `SyncBundle` (diff normal, como hoje) num arquivo opaco do repo (ex. `bundle.enc`), e um `pull` que não reconhece nenhum commit ancestral aplicado localmente **percorre o histórico de commits desde a raiz (ou desde o commit conhecido) e reaplica cada bundle em ordem** (`fold_into_manifest` já é idempotente/sequencial o bastante pra isso) — git dá de graça a listagem ordenada e completa do histórico que o GraphQL-by-owner do Arweave não dava sem paginação e sem tags. **Biblioteca escolhida pro v1: shell-out pro binário `git` do sistema** (`std::process::Command`), não `git2` (libgit2 traz OpenSSL/libssh2 nativos — conflita com a preferência já estabelecida por `rustls-tls` em todo o workspace, `reqwest.workspace` no `Cargo.toml` raiz, e com a dor já documentada de cross-compilar `openssl-sys` pro Android em P53/56) nem `gix`/gitoxide pro v1 (pure Rust, alinhado com `rustls`, mas suporte de push/rede historicamente menos maduro que libgit2 — reavaliar quando mobile entrar em pauta, já que mobile não pode fazer shell-out); shell-out também herda de graça a configuração de credencial/SSH agent que o usuário já tem no `~/.gitconfig`. **Trade-off aceito e já confirmado**: isso limita o v1 a desktop/CLI (onde existe um `git` instalável) — mobile fica fora até uma segunda rodada decidir entre `gix` embutido ou outro caminho. **Autenticação do MVP: HTTPS + token (PAT do Gitea/GitHub)**, não SSH/deploy-key — mais simples de configurar via um campo de texto colável (mesmo padrão de card didático já usado no Settings do desktop pra `storage_provider`, P61/Sessão 59), sem exigir geração/registro de chave SSH; token passado ao `git` via `GIT_ASKPASS`/credencial escopada só pro processo, nunca gravado no `~/.git-credentials` real do usuário. SSH/deploy-key fica pra v2. **Resolução de conflito**: rejeição nativa de push non-fast-forward do git é estritamente melhor que o `manifest_counter` last-write-wins de hoje — nunca sobrescreve silenciosamente um push concorrente — mas não é merge automático: como o arquivo commitado é um blob opaco cifrado, git não sabe mesclar dois bundles divergentes byte a byte; a resposta certa a um push rejeitado é `pull` (que já reconstrói o estado local via replay do histórico acima), recalcular o diff contra o manifesto agora atualizado, e tentar de novo — compõe naturalmente com o design de replay do item anterior, sem heurística de "quem ganha" nenhuma. **Escopo de MVP (v1, a implementar depois, não nesta rodada)**: `git.rs` novo em `warden-sync`, desktop+`warden-cli` só, HTTPS+token só, sem UI de Settings ainda (config via `config.toml`/flag, mesma postura inicial que outros campos novos tiveram em P61 antes de ganhar tela). **v2**: SSH/deploy-key, UI de Settings (card didático), mobile (reavalia `gix` nesse momento). **v3**: "viagem no tempo"/exploração de histórico antigo como feature de verdade pro usuário (hoje já emerge de graça como histórico de commits, mas virar UI navegável é escopo à parte); reconsiderar árvore de arquivos individuais com nomes ofuscados, só se um caso de uso real pedir diffing nativo por nota. **Ponto técnico ainda em aberto, a decidir só quando for implementar**: `SyncManifest` hoje é Arweave-flavored (`owner_address`/`last_tx_id`) — um backend git precisa de um ponteiro equivalente (provavelmente `remote_url`/`last_commit_sha`), e falta decidir se isso vira campos novos opcionais no mesmo `SyncManifest` (mais simples, mas mistura os dois backends num único struct) ou um enum de backend por dentro do manifesto (mais limpo, mais mudança). **Veredito desta rodada: vale implementar, mas com prioridade baixa** — resolve um pedido real do usuário (autonomia de infra, sem Web3/pago obrigatório) com reuso alto do que já existe (`bundle.rs`/`diff.rs`/`manifest.rs`/`pairing/` inalterados), mas o caminho Arweave/TruthID já funciona ponta a ponta e é o suportado primário hoje — não é bloqueio de nada, entra na fila atrás de outras pendências já em andamento. **v1 implementado (Sessão 62)**: `GitSyncEngine` novo (`crates/warden-sync/src/git.rs`) — push/pull via shell-out, replay de histórico real e testado (8 testes contra um bare repo local de verdade: push cria commit, pull replay total/parcial, push rejeitado sem `pull` antes e recuperação). `GitSyncConfig`/`FileConfig.git_sync` em `warden-bootstrap` (schema só, sem UI). CLI: `/sync git push`/`/sync git pull` (`commands.rs`/`interactive.rs`), parser testado. `cargo build/test/clippy --workspace` limpos. **Teste manual de ponta a ponta feito (Sessão 63, 2026-09-12)** — a lacuna acima foi fechada: dois processos `warden` reais (binário buildado, não mockado), cada um representando um device isolado (`XDG_CONFIG_HOME`/vault/`config.toml` próprios), controlados via um script Python com pseudo-terminal real (`pty.fork` + resposta fake ao `ESC[6n` que o `crossterm`/`ratatui` usa pra descobrir a posição do cursor, sem o quê a REPL trava esperando um terminal de verdade responder — achado no processo, não previsto no plano original) contra um repo bare local de verdade (`git init --bare`, sem host remoto real necessário — mesmo padrão hermético que os 8 testes automatizados da Sessão 62 já usavam). Roteiro completo confirmado com conteúdo real em disco, não só a mensagem da CLI: A empurra 1 arquivo novo (+ os 3 fixos do vault, seedados por `bootstrap()`), B puxa e recebe os 4 bytes-a-bytes idênticos; B cria um arquivo e empurra, A puxa e recebe; B avança o remoto de novo sem A saber, A edita local e tenta empurrar **sem** puxar antes → rejeitado com mensagem clara ("push rejeitado — outro dispositivo publicou primeiro; rode pull e tente de novo"), A puxa (resolve) e um novo push então funciona. `cargo test -p warden-sync -p warden-cli` seguiu 100% verde depois (nenhuma mudança de produção foi necessária — o motor funcionou de primeira via CLI de verdade). **Achado tangencial, não um bug do P63**: não existe hoje nenhum comando `/sync` no `warden-cli` equivalente ao `init_fresh()` que o desktop expõe (`sync_cmds.rs`) — só o desktop consegue gerar o primeiro `sync_secrets.json` de um device novo (Arweave **e** git compartilham essa mesma lacuna, já que os dois lêem o mesmo arquivo de segredos; não é algo que o P63 introduziu). Pra este teste, os secrets do device A foram gerados diretamente (mesmo formato JSON de `manifest::generate_secrets`) simulando um `init_fresh` já feito, e copiados pro device B simulando um pareamento já concluído — deliberado, já que o alvo desta rodada era validar o *transporte git*, não reexercitar o pareamento (coberto em outro lugar, agnóstico de transporte). Vale um item novo se algum dia incomodar de verdade (P63 v2, baixa prioridade, não registrado como pendência própria ainda). UI de Settings no desktop, SSH e mobile seguem v2/v3, não tocados | Pedido do usuário 2026-09-10, incorpora `warden-sync` (P37)/`StorageProvider` (P61) | 🟡 Baixa |
| P64 | **Geração de arquivos como entregável de conversa — documentos, planilhas, imagens, áudio e vídeo** — ideia trazida pelo usuário (2026-09-12), só pra registro nesta rodada, sem arquitetura nem prioridade decidida ainda. Quer que o Warden gere, sob pedido dentro de uma conversa: PDF, TXT, Markdown, XLSX e CSV — pro caso de planilha (XLSX) a fasquia é alta, não só um dump de dados, mas montar a planilha "bonita" de acordo com o que foi pedido, com **fórmulas de verdade** e formatação visual (cabeçalho, cores, largura de coluna), e o mesmo padrão de capricho vale pros outros formatos (PDF/TXT não devem sair como texto cru). Pra **imagem**, o usuário já assume que a geração em si só deve rolar via integração MCP (não um motor embutido no core) — mas quer que a **conversa consiga exibir essas imagens inline** quando um MCP gerar o arquivo, não só apontar o caminho. Mesma lógica pra **vídeo e áudio**: também via MCP, mas com "boa qualidade" — ou seja, o pedido não é só "conseguir chamar o MCP", é o software tratar mídia MCP-gerada como cidadão de primeira classe na UI da conversa (exibir imagem, tocar áudio, reproduzir vídeo direto ali), não devolver um caminho de arquivo cru pro usuário abrir por fora. Não existe hoje nenhum write path de deliverable em arquivo (documento/planilha/mídia) numa conversa — é ideia nova de ponta a ponta; provavelmente vira duas frentes distintas (motor de geração de documento/planilha vs. pipeline de exibir mídia MCP-gerada na conversa). Relaciona com a seção "Voz" deste arquivo (STT/TTS já existe, mas é fala em tempo real — isso aqui é arquivo de mídia como entregável, coisa diferente) e com "Tela de gerenciamento de integrações MCP" em `ROADMAP.md` (ambas dependem de MCP amadurecer no Warden). Ver `ROADMAP.md`, seção "Geração de arquivos como entregável (documentos, planilhas, imagens, áudio, vídeo)", pro detalhamento completo. **Escopo confirmado com o usuário antes de codar (Sessão 64, 2026-09-12)**: das duas frentes, atacar primeiro o motor de documentos/planilhas (não a exibição de mídia MCP-gerada, que fica pra outra rodada); arquivos gerados vivem numa pasta **separada do vault** (não syncam, não entram em `search`/`search_semantic`); escopo evolui do formato mais simples pro mais caro (TXT/MD → CSV → PDF → XLSX-com-fórmulas por último); nesta rodada a UI do chat não ganha nenhum affordance novo — a resposta do modelo só cita o caminho do arquivo, igual `write_file` já faz hoje. **Fatia 1 implementada (Sessão 64)**: tool nova `generate_document` (`crates/warden-core/src/tool/document.rs`), TXT/MD só — valida a extensão do `filename` (rejeita qualquer outra com mensagem explícita citando que PDF/CSV/XLSX vêm depois), escreve o arquivo, devolve `{"status":"ok","path":<caminho absoluto>}`. Registrada sempre (sem gate, mesmo nível de "sempre disponível" que `read_file`/`write_file` — não opt-in como `shell`). Diretório resolvido por `warden_bootstrap::resolve_generated_path` (`crates/warden-bootstrap/src/lib.rs`) — `FileConfig.generated_path: Option<String>` novo (config.toml only, sem UI/flag ainda, mesma postura que `enable_shell`/`delegate_max_depth` tiveram antes de ganhar tela) vence quando presente; senão deriva como **irmão do vault_path já resolvido** (`<vault_path>/../generated`) — mesma convenção de pasta humano-navegável que `desktop_default_vault_path()` já usa pro vault (`~/Warden/vault` → `~/Warden/generated`; CLI: `./vault` → `./generated`), escolhida deliberadamente pra não precisar adicionar um segundo parâmetro `default_generated_path` em `bootstrap()` (mudaria a assinatura em 6+ call sites — CLI/server/WhatsApp/Telegram/mcp-server/desktop). Desktop's `save_settings` carrega `existing.generated_path` adiante (mesmo tratamento que `git_sync`/`delegate_max_depth` já tinham) pra não apagar um valor editado à mão no `config.toml`. Verificado: `cargo test -p warden-core -p warden-bootstrap` (6 testes novos na tool + 3 em `resolve_generated_path`) e `cargo test --workspace` inteiro, ambos 100% verdes; `cargo clippy --workspace --all-targets` limpo. **Smoke real de ponta a ponta** (não só unitário): um example temporário rodou `bootstrap()` de verdade (chave dummy, sem chamar o modelo), confirmou `generate_document` na lista de tools do `Orchestrator`, chamou a tool de verdade e conferiu o arquivo real em disco no caminho esperado (`.../generated/smoke.md`, irmão do vault) — example removido depois, não versionado. **Ainda em aberto (antes da fatia 2)**: CSV, PDF, XLSX-com-fórmulas (cada um traz sua própria decisão de dependência nova — não escolhida ainda); UI de Settings pro `generated_path`; qualquer affordance no desktop pra abrir o arquivo gerado direto da conversa; a frente de exibir mídia MCP-gerada inline, não tocada nesta rodada. **Fatia 2 implementada (Sessão 64, 2026-09-12, continuação)**: `.csv` adicionado a `SUPPORTED_EXTENSIONS` no mesmo `GenerateDocumentTool` — sem dependência nova nem parsing/validação de verdade, já que CSV é texto puro e o modelo já entrega a string bem-formada (cabeçalho + linhas separadas por vírgula); mesmo caminho de escrita direta do TXT/MD, só a extensão aceita e as mensagens de erro/descrição da tool mudaram. Teste novo (`writes_a_csv_file`) cobrindo escrita de verdade em disco; `rejects_unsupported_extension`/`rejects_missing_extension` atualizados pra mensagem nova. `cargo test -p warden-core` (7 testes no módulo, todos verdes) e `cargo clippy -p warden-core --all-targets` limpos. **Fatia 3 implementada (Sessão 64, 2026-09-12, continuação)**: `.pdf` adicionado a `SUPPORTED_EXTENSIONS` — primeira fatia do P64 com decisão de dependência nova de verdade (CSV foi texto puro, não contou). **Biblioteca escolhida: `lopdf` 0.45**, não `printpdf` (versão atual, 0.12, reescreveu a API e passou a exigir embutir uma fonte TTF/OTF própria — nada mais de fontes-base do PDF sem embedding, o que obrigaria versionar um asset de fonte no repo só pra escrever texto simples; tem também um sistema de layout HTML descrito como "experimental" na própria doc) nem `genpdf` (mesma exigência de embedding, construído sobre uma versão antiga do printpdf). `lopdf` é inclusive o backend de baixo nível que o próprio `printpdf` usa por trás — permite referenciar uma das 14 fontes-base do PDF diretamente (`BaseFont => "Helvetica"`, sem embedding) exatamente como o exemplo oficial da lib demonstra, pure Rust, sem OpenSSL/native-tls na árvore de dependências (confirmado via `crates.io`) — mesmo raciocínio já usado pro `fastembed` (`crates/warden-core/Cargo.toml`) e pra escolha de shell-out de `git` em vez de `git2` no P63. Único efeito colateral aceito: `lopdf` traz `sha2 0.11` como transitiva, coexistindo com o `sha2 0.10` já pinado no workspace (duas majors compiladas, sem conflito, só peso extra de compilação — não valia forçar unificação só por isso). **Layout do PDF (v1)**: página A4 (595×842pt), margem 50pt, fonte Helvetica 11pt via `Encoding => WinAnsiEncoding` (sem embedding), leading 14pt — texto simples bem paginado, **sem parsing de Markdown/estilo** (a "fasquia alta" de capricho do P64 é pro XLSX, não pra esta fatia). Quebra de linha: `content.lines()` preserva parágrafos explícitos, cada linha resultante passa por *word-wrap* guloso usando uma heurística conservadora de largura média de glifo (`0.5 * font_size`, não é métrica AFM exata — deliberadamente generosa pra nunca estourar a página). Paginação: uma página a cada ~53 linhas (altura útil / leading), todas compartilhando o mesmo dicionário de `Resources` herdado do nó `Pages`, mesmo padrão do exemplo oficial do `lopdf`. Acentuação pt-BR: conteúdo UTF-8 transcodificado pra bytes WinAnsi — code points até U+00FF (todo o Latin-1 Supplement, cobre á/à/â/ã/ç/é/ê/í/ó/ô/õ/ú/ü e maiúsculas) mapeiam byte a byte porque WinAnsi concorda com Latin-1 nessa faixa; qualquer coisa acima disso (emoji, aspas tipográficas) vira `?` — limitação conhecida de v1, documentada em comentário, sem crash. Testado com round-trip de verdade, não só "não caiu": `writes_a_pdf_file` recarrega o arquivo gerado via `lopdf::Document::load` e confere `extract_text` (incluindo os acentos, prova que a transcodificação WinAnsi bate com o que o próprio `lopdf` decodifica de volta) e `writes_a_pdf_with_multiple_pages` força paginação de verdade e confere `get_pages().len() >= 2`. `cargo test -p warden-core` (9 testes no módulo, todos verdes), `cargo clippy -p warden-core --all-targets` e `cargo build --workspace`/`cargo test --workspace` inteiros, todos limpos. **Ainda em aberto**: XLSX-com-fórmulas, UI de Settings pro `generated_path`, affordance no desktop, frente de mídia MCP-gerada inline | Usuário (2026-09-12) | ⚪ A debater |

### Funcionalidades Pendentes

| ID | Item | Prioridade |
|---|---|---|
| P7 | **Todas as fases 1-10** — projeto recém-criado, nenhuma fase iniciada | — |

---

## Resolvidas

| ID | Item | Resolução | Sessão |
|---|---|---|---|
| P65 | **Pareamento por QR (Fase 9.7) — lado mobile sem build Android/emulador real** | **Toolchain Android completo instalado nesta máquina e build real confirmado** ✓. JDK 21 (Temurin), Flutter SDK, Android cmdline-tools/platforms/build-tools e a NDK — tudo instalado com o ok explícito do usuário a cada passo (SDK mínimo sem emulador; depois `rustup` separado do Rust do `pacman` pra dar ao `cargokit` os targets Android que faltavam). Bloqueio real no meio do caminho: a `warden/target` do Cargo (76GB!) e o `~/.gradle`/`~/.pub-cache`/`mobile/build` estavam todos no disco principal, que foi a 99% de uso (2.8GB livres) — causa raiz do disco cheio, não algo desta sessão. Resolvido **movendo tudo isso pro HD de 1TB** (`/mnt/hd1tb/dev-tools/`: `flutter`, `android-sdk`, `jdk-21.0.12.1+1`, `.gradle`, `.pub-cache`, `.cargo`, `.rustup`, `warden-cargo-target`, `warden-mobile-build`), com symlinks no lugar de sempre (`~/.local/opt/*`, `~/.gradle`, `~/.cargo`, `~/.rustup`, `warden/target`, `mobile/build`) — transparente pra qualquer ferramenta, disco principal caiu de 99% pra ~53% de uso. `JAVA_HOME`/`ANDROID_HOME`/`PATH` persistidos em `~/.bashrc`. Com espaço de sobra, `flutter build apk --debug` **compilou de verdade** — `warden_mobile_bridge` (a ponte Rust) built pras 4 arquiteturas Android, APK de 200MB gerado em `mobile/build/app/outputs/flutter-apk/app-debug.apk`. Ainda não testado: instalar o APK num emulador/celular real e escanear a câmera de fato (sem emulador instalado ainda) — lacuna menor, registrada só como nota, não como pendência nova, já que o build (o que P65 realmente cobrava) está confirmado | Sessão 61 (continuação) |
| P58 | **App Mobile — identidade visual não alinhada com a marca do Warden** — `ColorScheme.fromSeed(seedColor: Colors.deepPurple)` (roxo genérico do Material) em vez dos tokens reais que o desktop já usa | **Escopo fechado com o usuário antes de codar: só cores de marca** (não uma revisão visual mais ampla de layout/espaçamento/tipografia, que ficaria maior e sem sinal de prioridade) ✓ — `mobile/lib/main.dart` ganhou `theme`/`darkTheme`/`themeMode: ThemeMode.system`, com `ColorScheme.fromSeed` usando os mesmos tokens do `desktop/src/App.css` (`--color-accent`: `#7c3aed` no claro, `#a78bfa` no escuro) em vez do `Colors.deepPurple` genérico — antes só existia um `theme` único, sem suporte a modo escuro nenhum. Nenhuma outra mudança necessária: as telas (`ConnectionScreen`/`ChatScreen`/`SyncScreen`) já usam `Theme.of(context).colorScheme` em vez de cor hardcoded, então herdam a marca automaticamente (as poucas cores literais que existem — `Colors.grey/orange/green/red` em `connection_screen.dart` — são indicadores semânticos de status de conexão, não cor de marca, deixados como estavam). `flutter analyze`/`flutter test` (33 testes, nenhum novo — mudança é só configuração de tema) limpos. **Usuário pediu explicitamente pra não esperar teste no celular real antes de fazer a troca** — verificação visual numa janela/emulador real fica como pendência separada, ver P59 | Sessão 57 (2026-09-08) |
| P52 | **Estrutura padrão do vault + visualização pela interface** — parte fixa/padrão da memória (perfil, comportamento, feedback) + resto livre + fácil visualização pela interface | **Duas partes, mesma sessão de origem, sessões de execução diferentes** ✓. **Parte 1 (Sessão 57, estrutura fixa)**: 3 arquivos reservados na raiz do vault (`_profile.md`/`_behavior.md`/`_feedback.md`, `FIXED_VAULT_FILES` em `warden-core`), seedados com template curto em português na primeira vez que cada um é usado (idempotente), sempre injetados no prompt via `Vault::standing_memory()`/`Orchestrator::handle_turn_streaming` (independente de busca por relevância — todo canal herda de graça), excluídos de `search`/`search_semantic` (só na raiz) pra não duplicar. Verificado de ponta a ponta com o Gemini real (fato fictício em `_profile.md` refletido corretamente numa resposta). **Parte 2 (Sessão 57, continuação — visualização pela interface)**: tela "Vault" nova no desktop (`VaultView.tsx`, entrada na sidebar ao lado de Usage/Sync/Settings) — só leitura (edição continua por fora, Obsidian/editor de texto, ou a IA via `WriteFileTool`; editor completo fica pra uma v2 se fizer falta), com os 3 arquivos fixos destacados numa seção própria no topo da navegação (rótulos em português, mesma ordem de `Vault::standing_memory`), separados de uma árvore simples do resto do vault (pastas antes de arquivos, alfabética). Dois comandos Tauri novos (`desktop/src-tauri/src/vault_cmds.rs`, mesmo precedente de módulo dedicado que `sync_cmds.rs`): `list_vault_files` (reaproveita `Vault::list_all_files`, já ignora dotfiles, filtra fora os 3 fixos só quando na raiz) e `read_vault_file` (reaproveita `Vault::read`, caminhos só vêm do que a própria lista retornou, sem necessidade de sanitização extra). Conteúdo renderizado via `ReactMarkdown`/`remark-gfm`, dependência já existente no projeto (usada antes só em `MessageBubble.tsx` pras bolhas de chat) — `MarkdownLink` (abre link no navegador padrão em vez de navegar o webview) exportado de lá e reaproveitado, zero dependência nova. Arquivo fixo vazio/nunca escrito mostra um placeholder discreto em vez de erro; erro de leitura de um arquivo qualquer da árvore fica isolado ao painel de conteúdo, sem derrubar a navegação. `cargo check --workspace`/`cargo clippy --workspace --all-targets` e `npm run build` (tsc+vite) limpos. **Verificado via Playwright contra o dev server real** (`npm run dev`, mock de `list_vault_files`/`read_vault_file` via `addInitScript`) — seção fixa, árvore agrupada por pasta, seleção trocando o conteúdo renderizado (título/itálico/lista/negrito), placeholder de arquivo fixo vazio, claro e escuro, todos conferidos por screenshot. **Verificado também contra o vault real do usuário** (`npm run tauri dev`, sem mock, ~4min de build): app abriu sem crash, e os 3 arquivos fixos foram seedados de verdade em `~/Warden/vault/` (antes vazio) com o conteúdo exato do template — confirma que o bootstrap real (não só testes) resolve o path certo e escreve no vault de verdade | Sessão 57 (2026-09-08) |
| P45 | **Um agente por conversa, escolhido no início** — não permitir mais de um agente por conversa; ao criar uma conversa nova, primeiro passo é escolher o agente, travado depois | **`ChatArea.tsx` ganhou um `.agent-picker`** ✓ (Sessão 57) — substitui o `<select aria-label="Agent">` do `chat-header` (Sessão 43), que deixava trocar de agente a qualquer momento. `needsAgentPick = !hasMessages && !selectedAgentId` (nenhum estado novo — reaproveita `hasMessages` e o reset de `selectedAgentId` que já existiam) mostra cards clicáveis por `AgentEntry` (ou um direcionamento pra Settings, se `agents.length === 0`) no lugar do composer liberado; `MessageInput` ganhou `disabled={isSending || needsAgentPick}`. Escolhido o agente (ou numa conversa já com mensagens), o header mostra um rótulo somente-leitura (`.chat-header-label`) em vez do `<select>` — sem jeito de trocar depois. Escopo confirmado com o usuário antes de planejar: **só desktop** (Telegram/WhatsApp/mobile/CLI não têm — ou mantêm — seleção de agente, fora desta rodada); **agente obrigatório** (não existe mais opção "sem agente" numa conversa nova); **modelo/provider não é afetado**, continua editável a qualquer momento. Nenhuma mudança de schema/Rust — `Conversation.agent_id` já era por-conversa desde a Sessão 43, a trava é inteiramente de UX. Conversas antigas continuam funcionando normalmente (rótulo mostra o último agente usado, ou "No agent" se nunca teve um), sem migração retroativa. Verificado via Playwright contra o dev server real (`npm run dev`) com um agente cadastrado de verdade no `config.toml` do usuário — picker, escolha, trava pós-mensagem, caso zero-agentes e tema escuro todos conferidos por screenshot; `npm run build` (tsc+vite) e `cargo check --workspace` limpos. Corrigido de passagem um texto desatualizado em `SettingsView.tsx` ("conversations use no persona by default"), que não fazia mais sentido com o agente virando obrigatório | Sessão 57 (2026-09-08) |
| P6 | **Estratégia de busca semântica** — embedding local vs API externa, pra Fase 4 (quando o grep simples da v1 não bastasse mais) | **Embedding local via ONNX (`fastembed`, `AllMiniLML6V2`)** ✓ (Sessão 56) — decisão do usuário com trade-offs postos antes de codar: local mantém a busca offline, model-agnostic (não depende de qual provider de chat está configurado — Anthropic nem tem endpoint de embedding), sem custo por busca, sem mandar o vault pra fora só pra indexar. `Vault::search_semantic` novo (`crates/warden-core/src/memory/{mod.rs,semantic.rs}`), mesma `SearchHit` de sempre. Índice (`.warden/semantic_index.json`) vive dentro do próprio vault, dot-prefixado — já ignorado por `list_files`/`list_all_files`/sync de graça, sem tocar `warden-sync`. Self-healing (rehash + reembed incremental a cada chamada, cobre edição do vault por fora do Warden) em vez de hooks nos pontos de escrita. `Orchestrator::handle_turn_streaming` roda a busca semântica dentro de `tokio::task::spawn_blocking` (primeiro uso desse padrão no projeto) e cai pro grep original em qualquer erro — resiliente sem precisar de um toggle novo em `config.toml`/Settings. **Verificado de ponta a ponta com o modelo real baixado de verdade** (rede disponível neste ambiente) — ranking correto distinguindo "consulta médica" de "compromisso com dentista" sem nenhuma palavra em comum, e refresh incremental confirmado após editar um arquivo. `cargo test -p warden-core` (22 testes, incluindo os 2 reais de rede, `#[ignore]`d por padrão), `cargo check --workspace` e `cargo clippy --workspace --all-targets` limpos; `cargo test -p warden-sync -p warden-bootstrap -p warden-cli` (crates que mais exercitam `Vault`) também limpos. **`cargo test --workspace` completo não pôde ser confirmado nesta sessão** — o ambiente de dev ficou com o disco (`/home`) praticamente cheio (263MB livres) por causa do `target/` já grande (43GB) somado às dependências pesadas do `fastembed` (`ort`/`tokenizers`/`image`); mesmo depois de um `cargo clean` (48GB liberados), uma segunda tentativa de build completo esvaziou o `target/` sozinho no meio da compilação (aparenta ser alguma limitação de armazenamento do próprio ambiente, não relacionada ao código) — contornado verificando os crates individualmente em vez do workspace inteiro de uma vez. Fica registrado como P56 o fato de o caminho de fallback pro grep nunca ter sido exercitado contra uma falha de verdade (só o caminho feliz). Ver `ARCHITECTURE.md` pros detalhes técnicos completos | Sessão 56 (2026-09-08) |
| P53 | **App Mobile (Flutter) sem vault local nem participação no sync (P37)** — o motor `crates/warden-sync` só estava integrado no desktop e no `warden-cli`, ambos com acesso Rust direto; o mobile só tinha `SharedPreferences` | **Primeira ponte Rust↔Flutter do projeto, `crates/warden-mobile-bridge`** ✓ (Sessão 55) — casca fina sobre o mesmo `warden_sync::SyncEngine` do desktop/CLI (não expõe `warden-sync` direto via `flutter_rust_bridge`), funções `#[frb]` espelhando os 7 comandos Tauri de `sync_cmds.rs` (status/init/push begin+await/pull/pairing host+join), recebendo os 4 paths do sync (vault/config/secrets/manifest) como `String` explícitos vindos do Dart — mobile não tem `dirs::config_dir()` confiável em Android/iOS. Toolchain: `flutter_rust_bridge_codegen integrate` com backend **cargokit** (não Gradle/Xcode escritos à mão) — resolve NDK/ABIs automaticamente a partir do `Cargo.toml` do crate; `cargo-ndk` instalado, toolchain Android já presente (`~/.local/opt/android-sdk/ndk` 27/28) reaproveitado sem reinstalar nada. `SyncEngine`'s métodos async (Tokio: reqwest, tokio-tungstenite) rodam via um `tokio::runtime::Runtime` próprio (`OnceLock`) dentro do bridge, `.block_on()`'d de dentro de funções `pub fn` simples — `flutter_rust_bridge` já despacha qualquer `pub fn` não-`sync` pra uma thread de fundo por padrão, então bloquear ali nunca trava a UI do Dart. Paths do vault mobile resolvidos via `path_provider`'s `getApplicationSupportDirectory()` (não Documents, mesmo espírito do desktop usar o config dir do SO) — `mobile/lib/services/vault_paths.dart`. Nova tela `mobile/lib/screens/sync_screen.dart` (status, Initialize, Send/Pull com QR renderizado em Dart via `qr_flutter` a partir do `qr_payload_json` cru que o Rust devolve, pareamento host/join), alcançável por um ícone novo na `AppBar` da `ConnectionScreen` — sync não depende de estar conectado ao `warden-server`. **Decisão de produto, não só de teste**: campo opcional de "host override" na UI de pareamento (join) — `pairing_join_with_hosts`/`finish_push_with_hosts` já existiam no `SyncEngine` (só usados em teste antes) e o listener (`PairingHost::start`) já faz bind em `0.0.0.0`; o sweep automático de LAN não atravessa a fronteira NAT de um emulador Android (rede virtual `10.0.2.x`, distinta da LAN real do host) nem qualquer wifi com isolamento de cliente, então um override manual é uma necessidade real de produto, não só um hack de verificação. **Verificado de ponta a ponta contra hardware real (emulador Android), sem mock**: `.so` compilado pras 4 ABIs Android (`armv7`/`aarch64`/`x86_64`/`i686`) via `cargo-ndk` através do cargokit; APK debug instalado e aberto via `adb` no emulador `warden_test`; `bridge_status()` chamado de verdade on-device (tela renderiza "Paired: No" a partir do estado real, vazio, do storage do app); **pareamento real** contra um segundo processo (mesmo `SyncEngine` de produção, rodando fora do container do emulador, apontado pra um diretório de teste isolado — nunca tocou o `~/.config/warden/` real do usuário) usando o override de host (`10.0.2.2`) — confirmado via `adb shell run-as` que `sync_secrets.json` foi escrito de verdade no storage privado do app **com o `vault_key` idêntico** ao do processo host (prova a troca ECIES completa); e o **motor de diff/hash rodando de verdade dentro do `.so` compilado pra Android** — um arquivo escrito no vault local do emulador (`adb run-as ... touch`) fez `bridge_status` reportar `pending_vault_changes: 1` real. `cargo build/test/clippy --workspace` (incluindo o crate novo) e `flutter analyze`/`flutter test` (28 testes, nenhum quebrado pelas mudanças em `ConnectionScreen`) limpos. **Deliberadamente fora desta fatia, mesmas lacunas já aceitas em outras pendências**: push/pull reais contra Arweave/TruthID não testados (extensão natural de P38/P55, não uma pendência nova); lado iOS só com o scaffold do cargokit/FRB no lugar (cobre Android+iOS por padrão), nenhum build/teste — sem Xcode/macOS neste ambiente (mesma lacuna de P39/P44); nenhuma UI de navegação/edição do conteúdo do vault (nem desktop nem CLI têm isso hoje — o vault é editado via tools do orchestrator, não por um browser de arquivos dedicado, fora do escopo de "armazenamento local + participar do sync" que a 4.4 pedia) | Sessão 55 (2026-09-07) |
| P37 | **Direção nova: TruthID como pagador/publicador Arweave pro vault+config do Warden (substitui P24)** — reduzir a dependência de servidor sincronizando vault+`config.toml` entre devices via Arweave, usando a carteira do TruthID (`pin()`) como pagadora, tudo cifrado antes de sair do device | **Motor de sync completo, `crates/warden-sync`** ✓ (Sessão 54) — fecha as 3 decisões que ficaram em aberto na Sessão 50: (1) **formato do manifesto**: hash sha256 por arquivo do vault (`Vault::list_all_files()` novo em `warden-core`, cobre todo tipo de arquivo, não só `.md`) + hash do `config.toml`, comparados contra `SyncManifest` (`~/.config/warden/sync_manifest.json`) pra computar o diff; bundle de uma sincronização é um envelope JSON (arquivos mudados em base64 + deletados + config se mudou) cifrado como **um blob só** (`bundle.rs`, reaproveitando `warden_truthid::crypto::encrypt_pin_content`/`decrypt_pin_content` com uma chave derivada por HKDF com salt/info próprios, distintos do contexto do TruthID). (2) **descoberta de "última versão"**: como `pin()` não permite tags customizadas, resolvido consultando o GraphQL do Arweave (`transactions(owners:[...],sort:HEIGHT_DESC)`) pelo **endereço da carteira do TruthID** — aprendido uma vez (via `transaction(id:...){owner{address}}` sobre a tx do primeiro push) e propagado a outros devices pelo pareamento, sem precisar de nenhum ponteiro copiado à mão. (3) **identidade que deriva a chave do vault**: um segredo próprio do Warden (`SyncSecrets.vault_key`, 32 bytes, `~/.config/warden/sync_secrets.json`, nunca visto pelo TruthID/Arweave), gerado no primeiro device e espalhado pelos demais via um **protocolo de pareamento novo** — código curto (8 chars) + LAN, sem câmera/QR (decisão do usuário nesta sessão: como o pareamento agora só acontece entre dispositivos com vault — desktop↔desktop, ou desktop↔`warden-cli` headless — e não mais desktop↔celular, QR perdeu a vantagem natural que teria com uma câmera de celular do outro lado). Reaproveita só os primitivos genéricos de `warden-truthid` (ECIES, varredura de LAN), com uma faixa de portas própria e um listener de verdade (`warden-truthid` nunca teve um — só o lado que varre). **Bug real achado pelo próprio teste de ponta a ponta**: a primeira versão do pareamento propagava `last_tx_id` do host junto — isso fazia o `pull` do dispositivo recém-pareado achar que já estava "atualizado" sem nunca ter baixado nada; corrigido removendo esse campo do payload de pareamento, só a chave e o `owner_address` viajam. 34 testes automatizados (unitários + round-trips completos com telefone/gateway/par de pareamento falsos). Integrado no desktop (tela "Sync" nova, QR em SVG) e no `warden-cli` (`/sync`, QR em Unicode no terminal — funciona por SSH numa máquina sem tela). **Mobile deliberadamente fora desta fatia** (exigiria a primeira ponte Rust↔Flutter do projeto) — registrado como P53. **Nunca testado contra o TruthID/Arweave reais** — registrado como P55 (mesma lacuna aceita de sempre, agora com superfície maior que P38). Ver `ARCHITECTURE.md` pros detalhes técnicos completos | Sessão 54 (2026-09-06) |
| P35 | **App Mobile — troca de stack: Tauri Mobile revertido, Flutter decidido (Fase 7.1 recomeça do zero)** | **Scaffold antigo removido, 7.1 refeita em Flutter, verificada de ponta a ponta no Android** ✓ — `desktop/src-tauri/gen/android/` (git-tracked) removido junto com `bundle.android.minSdkVersion` do `tauri.conf.json` (config mobile-específica; o resto do scaffold Tauri — `crate-type`/`mobile_entry_point` — já era genérico desde o início do projeto, não mexido). O **toolchain Android em `~/.local/opt/`** (JDK 17, Android SDK/NDK, licenças já aceitas) foi **reaproveitado, não removido** — Flutter usa exatamente o mesmo toolchain (`flutter config --android-sdk`), então nada precisou ser baixado de novo desse lado. Flutter SDK (stable, 3.47.2) instalado sem sudo em `~/.local/opt/flutter` (clone raso do repo oficial, mesmo espírito "sem privilégio de root" da instalação anterior do Android SDK) — único obstáculo real foi a ausência de `unzip` no sistema (sem pacote instalado, sem sudo pra instalar): contornado com um shim (`~/.local/bin/unzip`) que traduz a chamada `unzip -o -q FILE -d DIR` que os scripts internos do Flutter fazem pra `bsdtar` (libarchive, já presente no sistema). Projeto novo criado em `mobile/` (raiz do repo, fora de `desktop/` — client separado, não mais uma extensão do app Tauri), `flutter create --org com.warden --project-name mobile --platforms android,ios mobile` → `applicationId com.warden.mobile`. **Verificado de ponta a ponta, mesmo rigor da tentativa anterior em Tauri Mobile**: `flutter build apk --debug` compilou de verdade (Flutter baixou sozinho o NDK r28c e o Build-Tools 36 que faltavam pro target), instalado via `adb install` no mesmo AVD `warden_test` já existente, app aberto via `adb shell monkey`, **screenshot real via `adb exec-out screencap`** confirmando a tela padrão do Flutter (contador) renderizando corretamente — prova que a cadeia inteira (Dart → Gradle → APK → emulador) funciona neste ambiente. `mobile/.gitignore` (gerado pelo `flutter create`) já cobre `/build/`, `.dart_tool/`, `android/local.properties`, `android/.gradle` — nada disso versionado. **Lado iOS**: `flutter create` já gerou o projeto Xcode (`mobile/ios/`), mas sem Xcode/macOS neste container não há como buildar nem testar — mesma limitação que já existia com Tauri Mobile, agora registrada como P39 (nova) pro framework novo. Nenhum código de feature (7.2+) escrito — só o setup em si | Sessão 50 (continuação, 2026-09-06) |
| P1 | **Protocolo servidor↔cliente** — gRPC vs WebSocket vs HTTP | **WebSocket + protocolo JSON próprio, rodando dentro do túnel do Tailscale** ✓ — decisão confirmada com o usuário. Motivo: a Fase 9 (9.4/9.5) exige o servidor empurrar tarefas pro cliente certo e receber o resultado pela mesma conexão (duplex), que WS cobre nativamente com uma conexão persistente; gRPC exigiria protobuf/`tonic` nos dois lados (incluindo Tauri mobile) só pra tipagem forte que JSON simples já resolve, mesma família dos outros dois protocolos internos do projeto (sidecar do WhatsApp, MCP). **Suposição antiga corrigida**: a nota original de que dava pra "reaproveitar o relay stateless por WS do TruthID" era incorreta — o SDK real do TruthID (`TruthIDRequester`) não usa relay nenhum, é local-network sweep + IPFS/IPNS dead-drop ("no relayer, no TruthID server"), resolvendo pareamento sem VPN compartilhada, problema que o Warden não tem (já assume Tailscale). Nada do TruthID foi reaproveitado nesta decisão. Fecha P1 e desbloqueia tanto a 7.2 quanto a 9.2 (mesmo protocolo serve as duas). Formato exato das mensagens JSON, quem assume o papel de servidor, e autenticação da conexão WS ficam pra quando 7.2/9.2 forem implementadas de fato. Ver `ARCHITECTURE.md` | Sessão 50 (2026-09-06) |
| P33 | **Provider deletado deixa `providerId` de agente pendurado** — achado por `/code-review high` na Fase 6 (agentes/seletores da Sessão 43): `updateProvider` (Settings) já propagava rename de provider pro `providerId` de qualquer agente que o usasse (fix da Sessão 45, P32), mas `deleteProvider` só limpava `activeProvider` quando batia — nunca tocava nos agentes. `save_settings` (`lib.rs`) também só validava `active_provider` contra a lista de providers, nunca `agents[].provider_id`, deixando o estado inconsistente ser persistido sem erro. O verificador do code-review achou um terceiro ponto: `App.tsx::handleSelectAgent` setava `selectedProviderId` direto de `agent.providerId` sem o mesmo guard de existência já usado pra restaurar o provider de uma conversa (`App.tsx:60-61`). Efeito prático: apagar um provider usado como padrão por um agente, salvar sem aviso, selecionar o agente e mandar mensagem — erro cru "model provider not found" na cara do usuário | **Três fixes espelhando o padrão já usado pro rename** ✓ — `deleteProvider` (`SettingsView.tsx`) agora zera o `providerId` de qualquer agente que apontava pro provider removido, em vez de deixar pendurado; `save_settings` (`lib.rs`) ganhou validação de `agents[].provider_id` contra a lista de providers (mesmo formato de erro do `active_provider`), recusando o Save em vez de persistir silenciosamente; `handleSelectAgent` (`App.tsx`) ganhou o mesmo guard de existência já usado na restauração de conversa antes de aplicar o provider padrão do agente. `cargo build/test/clippy --workspace` e `tsc`/`npm run build` limpos | Sessão 46 (2026-09-04) |
| P32 | **Agentes nomeados + seletor por conversa (Sessão 43) sem teste de ponta a ponta na janela real** | **Confirmado contra o Gemini real, no processo achando e corrigindo dois bugs de verdade** ✓ — o usuário abriu o app (`npm run tauri dev`), cadastrou a chave Gemini real em Settings, criou o agente "pirata" e mandou mensagem: resposta refletiu a persona, confirmando `handle_turn`/os seletores/a persistência por conversa da Sessão 43 de ponta a ponta pela primeira vez. Dois bugs reais surgiram no caminho (nenhum coberto pelos mocks da 43, ambos corrigidos na mesma sessão, ver `ARCHITECTURE.md`): (1) **`SettingsView.tsx`** — o campo "Name" de um provider edita `provider.id` direto, mas nada sincronizava `activeProvider`/`agent.providerId` numa renomeação; o usuário criou o provider (id automático `provider-1`), renomeou pra `gemini`, e `active_provider` ficou órfão apontando pro nome antigo — `save_settings` aceitava e só quebrava depois, ao mandar mensagem (`active_provider 'provider-1' not found among configured providers`); (2) **`gemini.rs`** — `gemini-3.5-flash` é um modelo "thinking" que anexa um `thoughtSignature` opaco a uma part de `functionCall`, e a API rejeita (400 INVALID_ARGUMENT) qualquer turno seguinte que não o reenvie no mesmo lugar; o `ToolCall` interno não carregava esse campo, então nenhuma chamada de tool sobrevivia a um segundo turno — surgiu batendo direto no `delegate_task`. Ver `ARCHITECTURE.md` pros detalhes de cada fix | Sessão 44 (2026-09-04) |
| P3 | **Formato do prompt de sistema** — persona configurável do agente | **Texto livre, registry de agentes no `config.toml`** ✓ — decisão explícita do usuário: agentes vivem no `config.toml` (não como arquivos no vault), mesmo padrão flat-list/`id`-dobra-como-nome já usado pros providers de modelo (Sessão 35). `AgentConfig { id, persona, provider_id }` novo (`warden-bootstrap`) — `persona` é texto livre, sem nenhuma estrutura/parsing imposta, enviado verbatim como mensagem de sistema. `Orchestrator::handle_turn` novo (`warden-core`) — `handle_message`/`handle_message_with_attachments` viram wrappers finos dele (mesmo padrão da Sessão 40), aceita um `system_prompt: Option<&str>` opcional, dá push como a primeira mensagem quando não-vazio, antes do contexto do vault. Zero mudança de comportamento pros 3 canais existentes (CLI/Telegram/WhatsApp) e pro `DelegateTool`, que continuam passando `None`. Fecha de vez a pendência original de "como estruturar a persona" — a resposta foi "não estruturar, deixar o usuário escrever livremente". Ver também a resolução conjunta com o seletor por conversa, registrada abaixo | Sessão 43 (2026-09-03) |
| — | **Seletor de agente e de modelo por conversa** (pedido do usuário 2026-09-03, junto com P3) | **Dois `<select>` numa barra nova `.chat-header`, acima do chat** ✓ — decisão confirmada com o usuário: trocar agente/modelo no meio de uma conversa afeta só as mensagens daí pra frente (o histórico já mostrado na tela não muda de dono). `Orchestrator` ganhou `with_model(model) -> Self` (barato — `model`/`vault` são `Arc`, clonar é só bump de refcount), usado pelo desktop pra trocar o modelo de uma chamada específica sem re-rodar `bootstrap()` inteiro (que reconectaria MCP servers, refaria OAuth, etc. — caro demais por mensagem). `build_model_provider` (antes `fn` privada em `warden-bootstrap`) virou `pub` pra o desktop poder construir um `ModelProvider` avulso a partir de um `provider_id` qualquer. IPC `send_message` ganhou `agent_id`/`provider_id: Option<String>` (frontend manda `null` explícito quando não quer override, nunca omite a chave) — só reconstrói o modelo quando `provider_id` de fato difere do provider ativo, evitando trabalho à toa no caso comum. `Conversation` (`warden-bootstrap`) ganhou `agent_id`/`provider_id: Option<String>` (`#[serde(default)]`, mesma retrocompatibilidade de `usage`/`attachments`) — grava a última seleção pra restaurar ao reabrir a conversa; se o id salvo não existir mais (agente/provider apagado depois), a UI cai pro default (`App.tsx`, `useEffect` chaveado em `activeConversationId`+`settings`). Settings ganhou uma seção "Agents" (`AgentCard`, mesmo mirror de "Model providers"): nome, `<textarea>` de personalidade, `<select>` de modelo padrão (só pré-preenche o seletor da conversa quando o agente é trocado, não é enforced depois). `cargo build/test/clippy --workspace` limpos — testes novos em `orchestrator/mod.rs` (`handle_turn_prepends_the_persona_as_the_first_message`, `handle_turn_ignores_a_blank_persona`, `with_model_swaps_the_model_used_without_touching_the_original`) usando os mesmos mocks já existentes no arquivo. `tsc`/`npm run build` limpos; layout dos dois seletores na `.chat-header` e do `AgentCard` na Settings conferido via screenshot de harness estático, claro e escuro. **Teste de ponta a ponta contra um provider real ainda não feito** — registrado como P32 | Sessão 43 (2026-09-03) |
| P28 | **TTS na resposta do desktop** (metade "output" de P28 — fecha a pendência de vez, as três metades feitas) | **Síntese sob demanda via a mesma API de voz da OpenAI, acionamento manual** ✓ — decisão explícita do usuário: um botão por mensagem do assistente (não autoplay), e reaproveitar a mesma chave `whisper` já existente em vez de pedir uma segunda (`/v1/audio/transcriptions` e `/v1/audio/speech` são a mesma conta OpenAI). Novo módulo `crates/warden-core/src/speech.rs` (espelha `transcribe.rs`): `POST /v1/audio/speech` (`model: "tts-1"`, `voice: "alloy"`, fixos por enquanto), devolve os bytes crus do mp3 (sem envelope JSON pra desserializar, ao contrário da transcrição). IPC novo `synthesize_speech` no desktop, mesma forma `AttachmentPayload` mime+base64 de sempre. `MessageBubble.tsx` ganhou um botão de áudio (`SpeakerIcon`/`StopIcon` novos) junto da contagem de tokens, com três estados (idle/loading/playing) — toca via `<audio>` do próprio browser, clique durante a reprodução para e reinicia do zero na próxima vez (toggle tocar/parar, não play/pause). Texto sanitizado antes de ir pro TTS por um util novo, `desktop/src/lib/stripMarkdown.ts` (regex simples — sem dependência nova tipo `remark`/`strip-markdown`, consistente com o resto do projeto), senão o modelo de TTS lê a marcação markdown em voz alta. Doc comment do campo `ApiKeys.whisper` atualizado pra deixar claro que cobre as duas pontas agora. `cargo build/test/clippy --workspace` e `tsc`/`npm run build` limpos (sem teste unitário novo no `speech.rs` — resposta é bytes crus, não há wire-shape JSON pra testar, ao contrário de `transcribe.rs`); botão conferido nos dois estados (idle e "tocando") via screenshot de um harness estático reaproveitando o `App.css` de verdade, claro e escuro. **Teste de ponta a ponta com uma chave de voz real ainda não feito** — registrado como P31 | Sessão 42 (2026-09-03) |
| P28 | **Input de voz no chat do desktop** (metade "input" de P28 — TTS na resposta segue em aberto) | **Transcrição universal via Whisper, dedicada e independente do provider de chat ativo** ✓ — decisão explícita do usuário: em vez de áudio nativo por provider (só Gemini/OpenAI recente suportam, Anthropic não), um passo de STT roda sempre antes, então funciona igual não importa qual dos 3 está ativo. `ApiKeys.whisper: Option<String>` novo (mesmo padrão do `tavily`, chave dedicada, independente do registry de providers de chat); novo módulo solto `crates/warden-core/src/transcribe.rs` (não é `ModelProvider` nem `Tool` — roda antes de `handle_message`, o modelo nunca invoca isso) faz `POST multipart/form-data` pra `/v1/audio/transcriptions` (`model: "whisper-1"`), `reqwest` ganhou a feature `multipart`. IPC novo `transcribe_audio` no desktop (reaproveita o tipo `AttachmentPayload` da Sessão 40 — mesma forma mime+base64). Composer ganhou botão de microfone (`MicIcon` novo). **Gravação em si passou por duas tentativas na mesma sessão**: a primeira, `MediaRecorder`/`getUserMedia` do browser, foi testada pelo usuário na janela real e falhou — WebKitGTK nega `getUserMedia` por padrão no Linux, limitação conhecida do Tauri/wry sem solução oficial (ver `ARCHITECTURE.md`). Trocada por **captura nativa via `cpal`**: novo módulo `desktop/src-tauri/src/recording.rs` grava numa thread OS dedicada (o `cpal::Stream` nunca sai dela — não é `Send` de forma confiável entre plataformas), novos comandos IPC `start_recording`/`stop_recording` substituem o `MediaRecorder`, WAV codificado com `hound` e devolvido como o mesmo `AttachmentPayload` de sempre — `transcribe_audio` não mudou nada. Preenche o texto transcrito no campo de mensagem pro usuário revisar antes de enviar (não manda sozinho). Settings ganhou o campo "Whisper API key (voice input)" (mesmo componente `ApiKeyField` do Tavily). `cargo build/test/clippy --workspace` e `tsc`/`npm run build` limpos (1 teste novo, só desserialização da resposta — sem infra de mock HTTP no projeto, mesmo padrão da Sessão 40); layout do botão/estado "gravando" conferido via screenshot Playwright. **Teste de ponta a ponta (chave Whisper real + captura nativa de verdade) ainda não feito** — registrado como P30 | Sessão 41 (2026-09-01) |
| P28 | **Anexo de imagem no chat do desktop** (metade de P28 — áudio segue em aberto) | **Multimodal de verdade, imagem-only** ✓ — `warden_core::model::Attachment { mime_type, data }` novo, `Message` ganhou `attachments: Vec<Attachment>` e o construtor `user_with_attachments`; os três providers (`OpenAiProvider`, `AnthropicProvider`, `GeminiProvider`) codificam isso no formato multimodal nativo de cada um (`image_url` com data URI, bloco `image`/`base64`, `inlineData`, respectivamente) — verificado com testes unitários de serialização (sem mock de HTTP, seguindo o padrão do resto do projeto) checando o JSON exato que cada provider monta. `Orchestrator::handle_message` continua igual (usado por CLI/Telegram/WhatsApp/`DelegateTool`); ganhou um irmão `handle_message_with_attachments` que só o `send_message` do desktop chama. IPC novo `read_attachment` (lê o arquivo escolhido no diálogo, base64-encode, rejeita qualquer extensão que não seja png/jpg/jpeg/webp/gif com "Unsupported file type"). `MessageInput.tsx` ganhou botão de anexo (`AttachIcon` novo) + preview com thumbnails removíveis; `MessageBubble.tsx` mostra a imagem na bolha do usuário; anexos são persistidos no JSON da conversa (`ConversationMessage.attachments`, `#[serde(default)]` pra retrocompatibilidade) e re-enviados no histórico das voltas seguintes, senão o modelo perde a imagem depois da primeira resposta. Documentos genéricos (PDF/etc) ficaram deliberadamente de fora — ver P28 (não resolvida) pro porquê. `cargo build/test/clippy --workspace` e `tsc`/`npm run build` limpos; layout conferido via screenshot Playwright (claro e escuro). **Teste de ponta a ponta contra um provider real ainda não feito** — sem chave de API neste shell pra bater numa API de verdade — registrado como P29 (ver "Não Resolvidas") | Sessão 40 (2026-09-01) |
| P26 | **Sem client OAuth pro transporte HTTP do MCP** | **Client OAuth de verdade** ✓ — `rmcp` atualizado pra 3.1.4 (`cargo update -p rmcp`, `Cargo.toml` já pedia `"3"`) com a feature `auth` ligada, que já traz discovery (RFC 9728/8414), Dynamic Client Registration, PKCE e renovação automática de token prontos — nada disso foi reimplementado na mão. Novo módulo `crates/warden-core/src/tool/mcp_oauth.rs`: `connect_http_oauth` (headless, usado em todo `bootstrap()`, falha com mensagem clara se não houver token guardado) e `authorize_interactively` (fluxo interativo — abre o browser via `tauri_plugin_opener`, escuta um redirect local numa porta efêmera, troca o código por token, persiste). Token guardado em texto puro, um arquivo JSON por server (`~/.config/warden/mcp_oauth/<nome>.json`, mesma postura de segurança que as outras credenciais do Warden hoje). `McpServerConfig::Http` ganhou um campo `oauth: bool` (default `false`, compatível com todo `config.toml` escrito desde a Sessão 36); Settings ganhou um checkbox "Requires OAuth" com botões Connect/Disconnect, e o preset do Slack passou a usar isso em vez de "cole seu token". Verificado de ponta a ponta contra um server OAuth real (não mockado) — `crates/warden-core/tests/mcp_oauth.rs`: discovery, Dynamic Client Registration, o consentimento do browser (simulado com uma requisição HTTP real batendo no listener local, já que nada automatizado consegue clicar "Allow" de verdade), a troca PKCE, persistência em disco, e reconexão headless usando só o token salvo — tudo passou na primeira execução real. **Ainda não testado contra o Slack real** (exigiria um workspace Slack e um humano aprovando no browser) — se o Slack não suportar Dynamic Client Registration na prática, falta um caminho de client_id pré-cadastrado (`AuthorizationRequest::with_preregistered_client` já existe no `rmcp`, só não foi exposto na UI). Ver `ARCHITECTURE.md` pros detalhes e trade-offs (inclusive a segunda versão do `reqwest` que isso confirma, na mesma linha da Sessão 36) | Sessão 37 (2026-08-31) |
| P25 | **Client MCP (`McpToolProvider`) só fala transporte stdio** | **Transporte HTTP adicionado** ✓ — `McpToolProvider::connect_http(name, url, headers)` novo (`crates/warden-core/src/tool/mcp.rs`), `McpServerConfig` virou um enum `#[serde(untagged)]` (`Stdio`/`Http`) que continua parseando qualquer `config.toml` antigo sem mudança, e a UI de Settings do desktop ganhou um select "Transport" no card de cada MCP server. Verificado de ponta a ponta contra um server HTTP real local (`crates/warden-core/tests/mcp_http.rs`, `axum`+`rmcp` server-side, não mockado) — inclusive um teste provando que os headers customizados chegam de verdade no server (conexão falha sem o header exigido, funciona com ele). **Parcial**: cobre servers que autenticam com um token estático, mas não implementa um client OAuth — o Slack especificamente ainda não tem um caminho de "um clique" (ver P26 nova). Ver `ARCHITECTURE.md` pros detalhes e trade-offs (inclusive a dependência nova em `aws-lc-rs`/`cmake` que isso introduziu via `rmcp`) | Sessão 36 (2026-08-31) |
| P2 | **Framework de CLI** — clap vs structopt vs gum | **clap v4 (derive)** — structopt descontinuado/incorporado ao clap; gum não se aplica a Rust | Sessão 2 (2026-08-01) |
| P20 | **Trait `Channel` ainda não existe** — `PHASE.md` (etapa 2.2) previa uma trait `Channel` unificando canais externos; a Fase 2 (Telegram) optou por uma função concreta reutilizável (`handle_turn`) em vez disso, já que com um único implementador uma trait não validaria a forma certa | **Continua sem trait, decisão fechada** — com o WhatsApp (Fase 3) como segundo exemplo real, confirmado que o que é de fato compartilhável já está em `handle_turn`; os loops de recebimento (poll HTTP com offset vs stream de eventos sobre stdio) continuam genuinamente diferentes o bastante pra uma trait não reduzir duplicação real. Ver `ARCHITECTURE.md`. Reabrir se um terceiro canal mostrar um padrão diferente | Sessão 29 (2026-08-09) |
| P14 | **O que realmente exige servidor** | **Mapeado** em `ARCHITECTURE.md` ("Mapa de dependência de servidor") — só a Fase 9 (rede de nós/execução remota) exige de fato o papel servidor; canais Telegram/WhatsApp precisam só de "algo sempre ligado" (não é topologia servidor↔cliente); Warden API (P12) e Warden como server MCP (P11b) só precisam de servidor se chamados de fora do device. Pode ser revisado quando a Fase 9 for implementada de verdade | Sessão 7 (2026-08-02) |
| P5 | **Estratégia de busca no vault** — grep simples vs fuzzy finder vs embedding (Fase 4) | **Grep simples (substring, case-insensitive, por palavra)** para v1 — já implementado em `Vault::search`. Fuzzy/embedding fica pra Fase 4 (ver P6) se o grep simples não bastar | Sessão 8 (2026-08-02) |
| P22 | **Suporte a múltiplos provedores de modelo** | **Registry de provedores** (`ProviderConfig`/`FileConfig.providers`+`active_provider`, `warden-bootstrap`) ✓ — Anthropic dedicado (`AnthropicProvider` novo) + `Provider::OpenaiCompatible` genérico (`base_url` configurável no `OpenAiProvider`) cobrindo Ollama e qualquer outro server OpenAI-compatível de graça. Ver `ARCHITECTURE.md` pros detalhes e trade-offs | Sessão 35 (2026-08-29) |
| P23 | **Gerenciamento de API keys no desktop é só um campo de texto por provedor** | **Tela de Settings redesenhada** ✓ — lista de cards, um por provedor configurado, com nome/tipo/API key/base URL/model editáveis, seleção do ativo, adicionar/apagar. Verificado de ponta a ponta via Playwright headless (payload real conferido, zero erros de console). Ver `ARCHITECTURE.md` | Sessão 35 (2026-08-29) |
| P11 | **Tela de gerenciamento de integrações MCP** — (a) client MCP, (b) Warden como server MCP | **Ambas as direções resolvidas** ✓ — (a) nova seção "MCP servers" na tela de Settings do desktop: cards com nome/comando/argumentos/variáveis de ambiente, mais 4 presets de "quick add" (Filesystem, Google Workspace, Notion, GitHub via Docker) pra começar de um comando já pronto; (b) novo binário `warden-mcp-server` expõe todas as tools do orchestrator (`Orchestrator::tools()` novo) como um server MCP de verdade sobre stdio, pra qualquer client de terceiro (Claude Desktop, etc.) apontar pra ele. Verificado de ponta a ponta com um client MCP real conectando no binário real (não mockado). Ver `ARCHITECTURE.md` pros detalhes e trade-offs (GitHub via Docker, Slack ficou de fora — P25) | Sessão 35 (2026-08-29) |